# AGENTS.md

Codex를 포함한 코딩 에이전트가 이 프로젝트에서 따라야 할 규칙.

> **이 파일의 편집 기준**: 규칙을 추가하려면 “이 규칙이 없었으면 실제로 무엇을 틀렸을지”를 설명할 수 있어야 한다. 남길 가치가 있는 것은 ① 추론만으로 알 수 없는 프로젝트 사실, ② 검증 방법이나 작업 판단을 실제로 바꾸는 규칙이다. 불필요한 승인 절차는 만들지 않되, 이 문서의 Green/Yellow/Red 권한과 Plan 승인 조건은 반드시 따른다.

---

## 세션 시작과 상태 복원

모든 작업 세션 시작 전에 다음 두 문서를 먼저 읽는다.

- `docs/PROJECT_STRUCTURE.md` — 페이지 구조, JS 역할, 데이터 흐름의 인덱스
- `docs/PROJECT_STATE.md` — 현재 목표, 열린 작업, 미확정 판단, 다음 시작점

**세션 시작 최소주의:** 기본 절차는 위 두 문서 선독, `git status` 확인, 그리고 §0에 열린 작업이 있을 때의 연결 문서 확인까지만이다. 세션 시작을 프로젝트 전역 재감사로 확대하지 않는다. 과거 git log·커밋·감사 규칙·negative control은 현재 작업의 원인 파악에 직접 필요하거나 사용자가 명시적으로 요청한 경우에만 필요한 범위에서 조회·실행한다. 필독문서의 과거 감사·실패 기록은 참고사항이지 매 세션 자동 재실행 지시가 아니며, 현재 작업과 무관한 iframe·RLS·인증·migration·회귀검사를 예방적으로 재검증하지 않는다. 선독과 상태 확인 뒤에는 작업 위험도와 직접 관련성에 비례하는 검증만 하고 사용자 요청 작업으로 바로 진입한다.

`PROJECT_STATE.md §0`에 진행 중 작업이나 연결 문서가 명시돼 있으면 그 문서도 함께 읽는다. 진행 중 항목이 있으면 사용자에게 다음을 복원 보고한다.

- 현재 목표
- 보류 항목과 사유
- 미실행 항목
- 다음 작업 후보

사용자가 정정하지 않으면 이 복원 결과를 현재 상태로 간주하고 진행한다.

명확한 작업에 착수할 때는 문서와 사용자 요청에서 아래 세션 계약을 에이전트가 스스로 정리한다. 사용자가 체크리스트를 다시 작성하게 하지 않는다.

1. **목표** — 이번 작업이 바꿀 사용자 동작이나 결과
2. **종료 조건** — 끝났다고 판정할 수 있는 한 문장
3. **이번 범위** — 이번에 닫을 원자 작업과 실제 영향 경로
4. **제외 범위** — 무관한 UI·기능 개선으로 번지지 않게 그은 경계
5. **검증 기준** — 어떤 동작·데이터·화면으로 완료를 확인할지

문서가 충돌하거나, 프로젝트가 요구하는 승인이 필요하거나, 사용자만 알 수 있는 취향·사업 정보가 빠진 경우가 아니면 확인 질문으로 판단을 되돌리지 말고 근거 있는 제안을 제시하며 진행한다.

### 작업별 필독 문서

작업 주제가 바뀔 때마다 아래 문서를 읽고, 읽기 전에는 해당 영역을 수정하지 않는다.

| 작업 종류 | 필독 파일 |
|---|---|
| DB 테이블·컬럼·RPC·Storage | `docs/db-schema.md` |
| CottageDB 함수 / `window` 전역 API | `docs/js-api.md` (크로스파일 의존관계 포함) |
| localStorage / 세션 키 | `docs/ls-schema.md` |
| 인증·로그인·프로필·사진 | `docs/PROJECT_STRUCTURE.md` §3~5 |
| 이용시간·방문자·체류 추적 | `docs/PROJECT_STRUCTURE.md` §6 및 진행 중 Plan |
| 모달·iframe·시트 | `docs/PROJECT_STRUCTURE.md` §2-A |
| 게임 데이터·파이프라인 | `docs/PROJECT_STRUCTURE.md` §7~8 |
| BGG ID 수정 / `fetch:bgg` / 번역 파이프라인 | 작업 전 `npm run check`로 필요한 단계 확인 |
| UI/CSS·sticky·깜빡임 | `docs/DESIGN_RULES.md` |
| 업적·칭호·캐릭터·교환권·성장보드 | `docs/achievement-system.md` (SSOT) |
| 관리자 분석·방문/이용 집계·퍼널 | `docs/admin-analytics.md` (SSOT) |

---

## 저장 위치와 문서 관리

사용자가 “사례로 기록”하라고 하면 별도 형식 확인 없이 다음 순서를 기본 템플릿으로 사용한다: **증상 → 원인 → 해결 → 실패한 시도 → 재발 방지**. 해결이 검증되지 않았거나 원인이 가설이면 해당 상태를 명시하고, 해결된 것으로 단정해 기록하지 않는다.

같은 사실을 여러 문서에 반복 설명하지 않는다. 다른 문서에서 필요하면 복사하지 말고 정본을 가리킨다.

| 정보 | 정본 |
|---|---|
| 현재 목표·종료 조건·열린 작업·미확정 판단·다음 시작점 | `docs/PROJECT_STATE.md` |
| 완료한 변경과 검증 | git commit |
| 계속 유효한 시스템 구조·데이터 계약·위험 | 해당 도메인 문서 |
| 여러 세션에 지속되는 협업 원칙 | `AGENTS.md` |
| 세션 과정·틀린 가설·일회성 메모 | 임시 기록 — 작업 종료 시 폐기 |

### 상태 압축

- `PROJECT_STATE.md`에는 열린 상태만 남긴다. 완료 목록·커밋 목록·세션 회고를 중복해서 쌓지 않는다.
- 닫힌 항목은 원래 적혀 있던 위치에서 해결·보류·폐기 중 하나로 닫고, 완료 사실은 git과 해당 도메인 문서에 둔다.
- 잃으면 안 되는 기술 판단은 도메인 문서 한 곳에만 남긴다.
- 목표·종료 조건·다음 원자 작업을 최신 상태로 유지한다.
- 여러 항목을 닫았는데 `PROJECT_STATE.md`가 늘었다면 새로 열린 작업이나 필수 판단이 실제로 생겼는지 확인하고, 아니면 압축한다.

### 문서 생성과 체크포인트

- 추후 작업 목록은 `PROJECT_STATE.md`에서만 관리한다. `TODO.md`는 사용하지 않으며 `docs/TODO.md`가 있으면 삭제한다.
- 새 `*.md`를 만들기 전에 기존 문서에 흡수하거나 정본을 참조할 수 있는지 확인한다.
- 일회성 기록은 기존 문서에 최소한으로 남기거나 작업 후 폐기한다.
- 장기적으로 독립 관리할 필요가 있고 복원 비용이 높을 때만 `*_CHECKPOINT.md` 신설을 제안한다. 승인되면 `PROJECT_STATE.md §0`에 등록한다.
- 보류된 결정, 여러 후보안, 여러 페이지 비교, 대화에만 있는 판단 근거, 장시간 분석은 체크포인트 필요성을 높이는 신호다.
- 피드백 하나마다 별도 메모리 문서를 만들지 않는다. 같은 범주의 기존 규칙에 통합한다.

작업 중 운영 방식이 바뀌면 이번 세션에만 적용할 내용은 `PROJECT_STATE.md`의 현재 작업에 최소한으로 기록하고, 앞으로 계속 적용할 내용은 `AGENTS.md` 변경안으로 분리한다. 커밋 전 지속 규칙으로 승격할 것이 생겼다면 사용자에게 한 줄로 변경안을 묻고 Yellow 절차를 따른다.

규칙으로 승격하기 전에는 다음을 확인한다.

1. 여러 세션에 걸쳐 변하지 않는가
2. 기존 원칙에 이미 포함되지 않았는가
3. 사건 서사 없이 한두 문장으로 설명할 수 있는가
4. 프로젝트 상태나 기술 사실을 협업 원칙으로 잘못 올리는 것은 아닌가
5. git이나 도메인 문서에 이미 있는 내용을 반복하는 것은 아닌가

### 코드와 문서 동기화

변경은 같은 커밋에서 다음 순서로 닫는다.

1. 코드 변경
2. 해당 도메인 문서 즉시 갱신
3. `PROJECT_STATE.md`의 원래 열린 항목 정리
4. `git diff`로 코드·문서 정합성 확인
5. 함께 커밋

| 변경 사항 | 갱신할 파일 |
|---|---|
| DB 테이블·컬럼·RPC·Storage 변경 | `docs/db-schema.md` |
| CottageDB 함수 추가·제거·시그니처 변경, `window.xxx` 전역 추가·변경 | `docs/js-api.md` |
| localStorage 키·구조 변경 | `docs/ls-schema.md` |
| 페이지 파일 이동·추가·삭제 | `docs/PROJECT_STRUCTURE.md` |
| 인증·데이터 흐름 변경 | `docs/PROJECT_STRUCTURE.md` |
| UI/CSS 계약 변경 | `docs/DESIGN_RULES.md` |
| 기능·버그·작업 상태 변경 | `docs/PROJECT_STATE.md`의 원래 항목 |

버그·리팩토링·Plan 항목을 닫을 때는 같은 항목을 언급한 `PROJECT_STATE.md`, `REFACTOR_CHECKPOINT.md`, `PLAN_*.md`를 검색해 상태를 일치시킨다. 코드 수정 없이 재검증해 문제없음으로 닫은 경우도 “현재 함수 없음”, “정의 1곳”, “호출처 없음”처럼 재검증 근거를 원래 항목에 남긴다. 커밋 전 `rg`로 항목 ID와 핵심 함수명을 검색해 낡은 미해결 기록이 남았는지 확인한다.

---

## 닫기 우선과 작업 범위

작업 시작 전 완료 조건을 사용자에게 보이는 변화 기준으로 한 문장으로 정의한다. “파일 N개 수정”이나 “항목 N건 처리”는 완료 조건이 아니다.

여러 항목을 묶을 때는 같은 원인, 같은 코드 경로, 같은 검증 환경 중 하나를 실제로 공유해야 한다. 화면이나 주제가 비슷하다는 이유만으로 묶지 않는다. 각 항목을 독립적으로 진단한 뒤 공통성이 확인되면 묶는다.

우선순위는 다음과 같다.

1. 날짜가 정해진 마감
2. 현재 사용자 목표 안에서 확인만 하면 닫히는 것
3. 같은 근원과 검증 경로를 공유하는 묶음
4. 판단이 필요한 버그
5. 기능·구조·기획 작업

쉬운 작업이더라도 현재 목표 밖이면 목표를 바꾸는 근거가 아니다. 다른 작업으로 넘어가기 전에 현재 종료 조건을 확인하고, 못 채웠다면 미완 이유와 재개 조건을 기록한다.

항목을 닫는 방법은 세 가지다.

1. 에이전트가 코드·문서·검증으로 닫는다.
2. 사용자가 실기기·실계정에서 확인해야 하면 무엇을 어디서 어떻게 보면 되는지 한 번에 제시한다.
3. 사용자 취향·가치·사업 판단이 필요하면 근거와 추천안을 제시하고 결정을 요청한다.

“에이전트가 더 수정할 코드가 없다”는 이유만으로 세션을 끝내지 않는다. 2·3번에 해당하는 열린 항목을 사용자 눈앞에 꺼내야 닫을 수 있다.

### 옆 문제 판정

- 현재 버그와 **같은 원인·코드 경로의 증상**이면 현재 범위에서 함께 고치고 검증한다.
- 가치가 있지만 현재 목표와 별개면 `PROJECT_STATE.md`에 영향 대상과 수정·검증 경로를 갖춘 항목 하나로 등록하고 원래 작업을 계속한다.
- 크거나 되돌리기 어려우면 최선안을 제시하고 필요한 승인 후 실행한다.
- BUG FIX MODE에서는 무관한 다른 문제를 즉시 수정하지 않는다.
- 새 갈래가 흥미롭다는 이유만으로 따라가지 않는다.

발견을 기록할 때는 증상만 쓰지 말고 다음 두 가지를 함께 적는다.

- **영향 대상** — 바뀌는 실제 화면·출력·데이터·동작과 이를 읽는 소비자
- **수정·검증 경로** — 어디를 고치고 무엇으로 완료를 확인할지

### 작업 모드와 WIP

활성 작업은 동시에 3개를 넘지 않는다. 새 아이디어는 다음 작업 후보로만 기록한다.

**구현 작업**

- 먼저 동작하는 기능을 완성한다.
- 역할이 명확한 헬퍼만 추출하고, 1회성 로직을 과도하게 분리하지 않는다.
- 기존 변수명·패턴을 따른다.
- 대규모 구조 정리는 별도 리팩토링으로 분리한다.

**BUG FIX MODE**

- 리팩토링, 파일·함수 분리, 명명 변경, 무관한 CSS 정리를 섞지 않는다.
- 같은 근원 원인이 설명하는 증상은 함께 닫되, 별도 구조 개선은 후속 리팩토링으로 분리한다.
- `git revert`가 필요하면 즉시 멈추고 이유와 영향을 먼저 보고한다. 승인 없이 revert하지 않는다.

**REFACTOR MODE**

- 신규 기능, UI 변경, 정책 변경을 섞지 않는다.
- 기능 테스트가 완료된 뒤 별도 작업으로 진행한다.

---

## 승인과 Plan

### Plan이 필요한 작업

다음은 Plan을 작성하고 승인받은 뒤 실행한다.

- DB 테이블·컬럼·RPC 신규 생성 또는 변경
- localStorage 키·구조 신규 생성 또는 변경
- `window.CottageDB` 공개 API 추가·변경·삭제
- 인증 구조 변경
- 파일 삭제
- 대규모 리팩토링
- 구조 문서와 도메인 문서를 읽은 뒤에도 영향 범위가 불명확한 작업

### Codex 자체 우발 산출물

Codex가 이번 작업 중 명령 실행·로그 출력·디버깅·임시 변환으로 만든 것이 명백한 일회성 파일(잘못된 shell redirection 로그, ANSI 출력 찌꺼기, 임시 debug dump 등)은 내용과 생성 경위를 확인한 뒤 별도 Plan·승인 없이 정리할 수 있다. 기존 파일·사용자 파일·출처가 불명확한 미추적 파일, Git tracked 파일, `.env`·설정·사용자 데이터·업로드 파일처럼 중요할 가능성이 있는 파일은 대상이 아니다. 확신할 수 없으면 삭제하지 않고 사용자에게 보고한다.

Plan 형식:

```text
읽을 파일: [파일명:줄번호 범위]
변경할 대상: 기존 함수/키/테이블 X → Y (재사용 또는 신규, 신규면 이유)
새로 생성: 없음 / [생성 대상과 이유]
영향 파일: A.js, B.html
보존해야 할 기존 동작: [있으면]
위험요소: 오탐·충돌·예외 케이스와 첫 번째로 실패할 가능성이 높은 지점
롤백 방법: [되돌리는 방법이 필요한 경우]
```

승인 규칙은 판단을 미루는 핑계로 사용하지 않는다. 되돌리기 어려운 실행과 사용자 책임이 걸린 결정은 추천과 실행을 분리하되, 안전하고 범위가 명확한 진단·읽기·Green 작업은 묻지 말고 진행한다.

### 작업 권한

**Green — 별도 승인 없이 진행**

- CSS의 color, padding, margin, font-size 등 수치·값 변경
- 문구·오타 수정, 배너 교체
- `PROJECT_STATE.md`의 기존 항목에 사실과 상태 반영

**Yellow — 변경안 보고 후 승인**

- CSS 선택자 스코프·클래스·`position`·`z-index`·`display` 변경, 선택자 추가·삭제
- `AGENTS.md`, `DESIGN_RULES.md` 수정
- 새 `*.md` 생성
- `PROJECT_STATE.md` 섹션 추가·삭제 등 문서 체계 변경

**Red — Plan 후 승인**

- DB, 인증, 데이터 구조, 파일 삭제, 대규모 리팩토링

---

## 신규 생성 전 재사용 확인

리팩토링의 목적은 분리가 아니라 현재 규모에 맞는 가장 단순한 구조다. 유지·통합·분리·삭제 중 가장 단순한 해결책을 먼저 검토한다.

새 localStorage 키, DB 테이블·컬럼, 전역 함수, CSS 클래스를 만들기 전에:

1. `docs/ls-schema.md`, `docs/db-schema.md`, `docs/js-api.md`, `docs/DESIGN_RULES.md`에서 같은 용도를 검색한다.
2. 기존 항목을 확장할 수 있으면 재사용한다.
3. 신규 생성이 불가피하면 Plan이나 커밋 메시지에 이유를 남긴다.
4. 공용 헬퍼와 같은 개념을 일부 화면만 자체 계산하지 않게 소비처를 확인한다.

---

## 버그 진단과 파일 읽기

버그를 받으면 먼저 `증상 → 관련 함수 → 파일` 순서로 원인 가설을 짧게 정리한다. 초기 읽기 범위는 관련 파일 1~2개로 한정한다.

소스 파일을 읽기 전에는 다음 순서를 따른다.

1. `PROJECT_STRUCTURE.md`에서 파일 역할과 경로 확인
2. 해당 도메인 문서에서 함수명·계약·관련 줄 확인
3. `rg`로 실제 줄번호 확인
4. 필요한 줄과 주변 문맥만 읽기

이 선독 규칙은 실제로 지킨다. iframe·modal·sticky·navigation처럼 여러 파일과 렌더 경계를 거치는 작업은 수정 전에 실제 진입점 → 호출 경로 → 렌더 대상을 먼저 확인한다. 같은 영역을 수정했는데 사용자 화면에서 계속 실패하면 동일 함수나 selector를 계속 패치하지 말고 구조 문서와 실제 호출 경로부터 다시 확인한다. 사용자가 실제 화면에서 실패를 확인하면 기존 정적 검사나 이전 PASS 판단보다 사용자 관찰을 우선하고 기존 가설을 재검토한다.

“관련될 수 있는 모든 파일”을 처음부터 전부 읽지 않는다. 다만 문서와 코드가 충돌하거나 호출 경로가 여러 파일에 걸친 것으로 확인되면 필요한 범위까지 확장한다.

사용자가 반문하거나 기존 판단과 충돌하는 증거를 제시하면 코드·문서·커밋을 잠시 멈추고 재검증한다. 입장을 바꿀 때는 다음 중 무엇이 바뀌었는지 밝힌다.

- 새 증거·사실
- 새로 확인된 목표·제약
- 기존 정보의 누락·모순·오류

강한 표현만을 이유로 판단을 바꾸지 않는다.

---

## 검증

“될 것 같다”로 커밋하지 않는다. 작업 위험도에 비례해 검증한다. CSS·spacing·typography·문구처럼 데이터와 기능 동작에 영향을 주지 않는 단순 시각 수정은 기본 정적 오류만 확인하고 사용자 수동 검수를 전제로 완료한다. 데이터 저장·DB migration·인증·API·상태 변경처럼 실패 시 데이터나 기능에 영향을 주는 작업에만 강화된 검증을 수행하며, 사용자가 직접 검수할 수 있는 단순 UI 작업을 위해 별도 Playwright 테스트·test harness·회귀 검사기·문서를 만들지 않는다.

강화된 검증이 필요한 작업은 다음 중 영향에 맞는 하나 이상으로 실제 동작을 확인한다.

- 실제 데이터로 로직을 한 단계씩 추적
- Node 등 실행 가능한 테스트
- 0, null, 날짜 경계, SELECT 실패 등 엣지케이스 확인
- 브라우저에서 실제 상호작용과 렌더 확인
- DB·HTTP 응답의 상태·오류·행 수 확인

Plan이 필요했던 신규 기능·API·상태 변경은 커밋 전에 사용자 시점으로 한 번 더 훑는다. 코드 구조가 맞는지만 보지 말고, 숨겨진 버튼에 배지가 붙는지, 패널을 열었더니 알림이 사라지는지처럼 의도와 실제 동작이 어긋나는 경로를 확인한다.

원칙 점검을 요청받으면 문서가 존재한다는 사실만 확인하지 않는다. 해당 규칙의 존재·실제 선독·수정 전 적용 조건·완료 증거 요구를 각각 대조하고, 하나라도 빠졌으면 “규칙 미존재”와 “규칙 미적용”을 구분해 기록한다. 시각 작업의 사례는 원인과 실패한 시도를 기록할 수 있지만, 사용자 화면 또는 동일 조건의 런타임 측정으로 확인되기 전에는 해결 사례로 닫지 않는다.

### 검사기를 먼저 검증

- `0건`, `전부 통과`, `정상`은 검사기가 고장 난 결과와 구별되지 않는다.
- 가능하면 알려진 실패를 일부러 심은 **음성 대조군**으로 검사기가 잡는지 먼저 확인한다.
- 음성 대조군은 판정기가 작동한다는 것만 증명한다. 판정 구간에 증상이 발생할 표본이나 기회가 충분한지는 별도로 센다.
- 검사 결과가 알려진 사실과 충돌하면 사실을 바로 덮지 말고 검사기·환경·입력 범위를 먼저 재검증한다.
- 문자열과 주석에 있는 이름을 코드 참조로 세지 않는다.
- JS 전체를 손으로 만든 정규식 스캐너로 파싱하지 않는다. 질문을 좁힌 `rg`나 실제 파서를 사용한다.
- `rg`의 `\b`는 한글 조사 앞에서 기대대로 끊기지 않을 수 있다. 문서·주석 검색은 경계를 빼고 재확인한다.
- 검색 출력의 경로·슬래시·이스케이프가 의심스러우면 원문 줄을 다시 읽어 확정한다.

백슬래시·백틱·정규식이 포함된 검증은 heredoc이나 긴 `node -e` 같은 인라인 명령에서 셸이 내용을 바꿀 수 있다. 검토 가능한 스크립트로 실행한다. 서로 다른 셸과 런타임이 같은 임시 경로를 해석한다고 가정하지 말고, 실제 절대 경로를 확인한다.

### UI와 런타임 값

- 자동 assert는 질문한 것만 확인한다. 강화 검증 대상인 UI 동작을 수정한 경우 실제 화면이나 스크린샷을 **읽고** 확인한다.
- 데이터·기능 영향이 없는 단순 UI/CSS 수정은 브라우저 자동화 인프라를 추가하지 않고 사용자 수동 검수로 닫는다.
- 사용자가 스크린샷을 보내면 먼저 **주소·뷰포트·열린 화면·비교 기준**을 화면에서 식별하고, 그 증거를 현재 가설과 대조한다. 화면에 이미 있는 기준을 다시 요구하거나, 이전 요청의 대상에 묶여 현재 화면을 다른 페이지로 분류하지 않는다. 2026-08-27 초기의 추천 목록 요청에 가설을 고정한 채, 이후 스크린샷에 주소가 `pages/info/about.html`이고 본문이 1글자 폭으로 붕괴한 증거가 반복됐는데도 대상을 추천 패널로 오분류했다.
- 레이아웃·scroll·sticky·position·offset·CSS 변수는 추론만으로 고치지 않는다. `getComputedStyle()`, `getBoundingClientRect()`, `offsetHeight`, `clientHeight` 등 실제 값을 확인한다.
- 아코디언처럼 콘텐츠 높이가 바뀌는 UI는 `scrollTop` 자체를 보존 목표로 삼지 않는다. 사용자가 고정되어 보길 기대하는 target의 클릭 전후 viewport `top`을 같은 렌더 단계에서 측정하고, `afterTop - beforeTop`의 실제 부호로만 보정한다. 이벤트 capture 시점이나 첫 `requestAnimationFrame` 측정값을 최종 상태로 오판하지 말고, 전역 `scroll-behavior`와 sticky 기준 scroll container도 함께 확인한다.
- `calc()`·`var()`·`env()`가 섞인 값을 `parseFloat()`로 단순 숫자화하지 않는다. 실제 DOM 크기를 재거나 CSS가 처리하게 둔다.
- CSS·iframe·bottom sheet·깜빡임의 구체 규칙은 `DESIGN_RULES.md`와 `PROJECT_STRUCTURE.md §2-A`를 따른다.
- `style.css`를 브라우저 하니스의 `<style>`에 인라인할 때는 선두 BOM이 `:root`를 무효화할 수 있다. 실제 `<link>` 로드와 다른 증상이면 BOM과 origin 조건을 먼저 확인한다.
- 브라우저 자동화 산출물은 저장소 안에 쓰지 않는다. Live Server 감시 리로드를 피하도록 OS 임시 경로를 사용한다.

### 반복 패치 정지

같은 파일·함수·CSS 선택자·검증 스크립트를 두 번 수정했는데도 해결되지 않으면 즉시 값 추측을 멈추고 보고한다. 세 번째 수정 전에는 다음을 제시하고 승인받는다.

- 원인 가설
- 검증 방법
- 변경 예정 파일
- 롤백 방법

서로 다른 메커니즘의 두 수정이 완전히 같은 결과를 내면 “강도가 부족했다”가 아니라 가설 범주가 틀렸을 가능성을 먼저 본다. 세 번째 변형 대신 런타임 계산값과 환경 전제를 측정한다.

사용자가 한 번이라도 “여전히 같다”고 보고하면, 다음 패치 전에 현재 스크린샷으로 **대상 URL/파일·깨진 DOM·비교 컴포넌트·검증 뷰포트**를 다시 고정한다. 두 번 실패했다면 세 번째 수정 전에 대상과 기준 요소의 DOM 클래스·매칭 CSS 규칙·`getBoundingClientRect()`·계산된 padding을 같은 뷰포트에서 대조하고, **첫 시도 직전부터의 누적 diff**를 읽어 실패한 예외 규칙을 먼저 제거한다. 파일 수정이 아니라 검증 환경이 다른 중이면 환경부터 바꾼다.

반응형 UI에서는 수정하려는 규칙의 media query가 **현재 검증 뷰포트에 실제로 적용되는지**를 첫 번째로 확인한다. 예를 들어 `360px` 기기 에뮬레이션은 `@media (min-width:720px)` 내부의 PC 수정을 검증할 수 없다. 2026-08-27 추천 목록 패널에서 이 확인 없이 PC `max-width`만 연속 수정해 화면 변화가 없었다. 이후 같은 유형은 뷰포트·활성 규칙·실제 `getComputedStyle()` 값을 먼저 대조한 뒤에만 수정한다.

### 증거 없는 부재 판정 금지

grep 한 번으로 기능·동작·호출처가 없다고 단정하지 않는다. 정의, 간접 호출, 이벤트 바인딩, HTML 속성, 실제 런타임 중 필요한 경로를 확인한다. “없음”이나 “문제없음”으로 문서를 닫을 때는 재검증 근거를 남긴다.

---

## DB·데이터·보안

DB 작업 전 `docs/db-schema.md`, 공개 함수 작업 전 `docs/js-api.md`를 정본으로 읽는다.

### Supabase 오류 처리

- `supabase-js`는 쿼리 오류에서 예외 대신 `{ data: null, error }`를 반환할 수 있다. `try/catch`만 믿지 말고 `error`를 확인한다.
- 고칠 수 있는 실패는 위치를 식별할 수 있게 로그하고 기존 fallback 반환 계약(`[]`, `null` 등)은 유지한다.
- 정상적으로 허용되는 실패는 무조건 경고하지 말고 왜 삼켜도 되는지 주석을 남긴다.
- `.data`나 `.count`를 읽는 `Promise.all` 결과도 각 `.error`를 확인한다.
- DB/HTTP 조회는 상태 코드를 확인한다. 컬럼 오타의 400 응답을 0행으로 오독하지 않는다.
- 컬럼·필드명은 추측하지 말고 마이그레이션 SQL에서 확인한다.

### 행 수와 RLS

- 대량 조회가 정확히 1000행이면 PostgREST `max-rows` 절단을 의심한다. 현재 설정과 한계는 `db-schema.md`를 확인한다.
- 0행은 데이터 없음, 쿼리 오류, RLS 필터링, 테이블 없음으로 나눠 검증한다. 필요하면 `count: 'exact'`와 HTTP 상태로 대조한다.
- 이 프로젝트는 카카오 OAuth를 사용해 Supabase Auth의 `authenticated`/`auth.uid()`가 앱 사용자 인증으로 작동하지 않는다.
- **모든 테이블을 일괄 RLS DISABLE로 만들지 않는다.** `db-schema.md`에서 해당 테이블의 실제 계약을 확인한다. 새 테이블은 RLS 상태를 마이그레이션에 명시하고, RLS를 켜는 경우 필요한 작업별 `anon` 정책을 검증한다.
- DB 변경 전 기존 사용자가 잃을 데이터·권한·누적값을 먼저 확인한다.

### 데이터 보호

- 방문자·통계 버그 수정 시 데이터 초기화 금지. 원래 있어야 할 값을 복원하는 방식으로 처리한다.
- 브라우저 검증은 운영 DB 쓰기 여부를 먼저 확인한다. 가능하면 쓰기를 차단한다. 쓰기가 필요한 검증은 대상과 복원 절차를 명시하고, 종료 후 원복과 재확인을 수행한다.
- iframe 추적은 `PROJECT_STRUCTURE.md §2-A`의 기준을 따른다. 방문·체류는 부모 단위지만 iframe 안의 실제 사용자 행동 이벤트는 버리지 않는다.

### 영구 식별자

다음 ID는 사용자 데이터가 누적되는 기준이므로 배포 후 변경하지 않는다.

- `achievements.id`
- `game_play_records.game_id` (BGG ID)

이 ID들은 도감 완성률, 새 게임 경험 수, 캐릭터 해금, 플레이 통계의 기준이며 이미 사용자 자산이 연결돼 있다.

| 금지 | 허용 |
|---|---|
| `rabbit_20` → `rabbit_explorer_20`처럼 업적 ID 변경 | 업적 `name`·설명·포인트 변경 |
| 기존 플레이 기록의 BGG `game_id` 일괄 변경 | 신규 게임에 올바른 BGG ID 사용 |

ID 변경이 불가피하면 기존 ID 유지, 별도 migration, 기존 사용자 데이터 이전 방안을 포함한 Plan 승인 후 진행한다.

---

## 외부 의견과 시각화

GPT, Claude, Codex, Gemini 등 외부 AI나 제3자 의견은 구현 지시가 아니라 **문제 가설**로 취급한다.

사용자가 출처를 명시하지 않아도 긴 구조화 목록, 프로젝트 문맥이 없는 일반론 등 외부 AI 출력으로 보이는 의견은 같은 기준으로 재검증한다.

1. 현재 코드·문서·DB에서 문제가 실제인지 재검증한다.
2. 문제가 맞더라도 제안된 해결 모델을 그대로 정답으로 보지 않는다.
3. 프로젝트 목적·데이터 계약·기존 UI 구조에서 해결안을 다시 설계한다.
4. 외부 의견과 현재 구조가 다르면 차이와 근거를 보고하고 필요한 승인을 받는다.

표현·도형·지표를 바꿀 때는 그것이 주장하는 관계를 한 문장으로 적고 데이터가 보장하는지 센다. 순서·분기·포함관계가 증명되지 않으면 해당 도형이나 비율을 사용하지 않는다. 관리자 분석 작업에서는 `admin-analytics.md`의 분모·집계·단일 소스 규칙을 우선한다.

---

## Git과 협업

- 작업이 완료될 때마다 자동으로 커밋한다. 사용자가 이번 작업에서 커밋하지 말라고 명시하면 그 지시를 우선한다.
- 커밋 후 자동 push하지 않는다. push는 사용자가 명시적으로 요청한 경우에만 수행한다.
- feat와 refactor를 같은 커밋에 섞지 않는다.
- 여러 작업을 한 번에 받으면 건별 승인 질문 없이 연속 처리하되, 각 항목을 검증·커밋하고 무엇이 달라졌는지와 검증 결과를 그 자리에서 보고한다.
- **동일 worktree에서 여러 코딩 에이전트가 동시에 파일을 수정하지 않는다.** 병렬 분석은 가능하지만 수정 권한은 한 에이전트만 갖게 한다. 다른 세션·에이전트·사용자의 기존 변경은 소유자가 확실하지 않으면 덮어쓰거나 정리하지 않는다.
- 서브에이전트나 다른 에이전트가 작업한 경우 주 에이전트가 diff, 검증, 도메인 문서 갱신 여부를 직접 확인한다.
- `db-schema.md`, `ls-schema.md`, `DESIGN_RULES.md`, `PROJECT_STRUCTURE.md`가 바뀌면 완료 보고에 변경 파일을 표시한다. 사용자가 외부 프로젝트 지식 저장소를 운용한다면 갱신 대상임을 함께 알린다.

완료 보고는 함수명이나 항목 번호만 나열하지 말고 사용자가 보는 동작이 어떻게 달라졌는지 설명한다. 숫자에는 좋은 변화인지, 나쁜 변화인지, 아직 판정할 수 없는지를 붙인다. 진행 중 작업이나 미실행 항목이 있으면 다음을 구분한다.

1. 이번에 변경한 파일과 내용
2. 미실행 항목과 사유
3. 다음 후보 작업

---

## 이 규칙 집합의 감사

이 절은 `CLAUDE.md`에서 검증된 규칙을 Codex용 `AGENTS.md`로 이관한 근거를 보존한다. 오래됐다는 이유만으로 규칙이나 시행착오 기록을 삭제하지 않는다. 삭제 후보는 실제 발동 기록과 다른 SSOT의 보존 여부로 판단한다.

### 과거 감사 결과

- 2026-07-20 최초 감사에서 규칙마다 “없었으면 틀렸을 사건”을 대조해 11건을 잘라냈다. 삭제된 규칙과 근거는 `docs/_archive_claude_rules.md`에 있다.
- 2026-07-21 1차 감사: 잘라낸 11건은 삭제 유지, 「닫기 우선」과 평문 완료 보고는 유지 확정.
- 2026-07-22 2차 감사: 되살린 규칙 0건. 커밋 41개에서 검사기 의심 24회, 육안 확인 12회, 외부 의견 재검증 6회, 닫기 우선 11회, 옆 문제 판정 2회, 부재 실측 2회, SSOT 2회, 재사용 1회가 확인됐다.
- 2026-08-10 3차 감사: 되살린 규칙 0건, 조건부 유지 0건, 자를 후보 0건. 커밋 315개에서 검사기 의심 19회, 음성 대조군 12회, 육안 확인 18회, 실측 52회, 외부 의견 1회, 닫기 우선 5회, 옆 문제 판정 2회, 저장 위치 7회, 재사용 4회, 반복 패치 정지 4회, RLS 10회, 부재 실측 6회, Plan 1회, 런타임 값 검증 9회가 확인됐다.
- 다른 SSOT로 이동한 규칙도 3차 감사 구간에서 sticky 13회, 바텀시트 6회, 모달 겹침 5회, iframe 24회, z-index 3회, 센터모달 4회가 확인됐다. 이름이 아니라 실제 판단 내용을 센 결과다.
- 2차 감사는 세션 횟수 기준 트리거를 놓쳐 날짜 기준으로 바꿨고, 3차 감사도 여러 세션 미뤄진 뒤 사용자가 직접 꺼내 실행됐다. 날짜만 적고 세션 시작 시 확인하지 않으면 다시 누락되므로 이 파일의 세션 시작 절차에서 감사 트리거를 함께 확인한다.
- 대화에서만 발동하는 “제안이지 질문이 아니다”, “반문 시 재검증”, “평문 완료 보고”는 커밋 로그 0회만으로 무효 판정하지 않는다. 측정 불가는 무발동이 아니다.
- 이동한 규칙은 커밋에 문서 이름이 적혔는지가 아니라 규칙의 **내용이 실제 판단에 나타났는지**로 평가한다.
- 2026-08-31 기록 센터모달 작업에서 `PROJECT_STRUCTURE.md` 선독 규칙을 실행하지 않은 채 부분 코드와 정적 검사만 보고 실제 iframe 렌더 경계를 잘못 추정했고, 동일 영역을 반복 패치하면서 원인 파악과 토큰 사용이 불필요하게 늘어난 사례. sticky의 실제 상위 여백·호환 CSS를 함께 대조하지 않았고, 이벤트 capture/첫 `requestAnimationFrame` 값을 최종 상태로 오판했으며, `scrollTop`을 목표로 삼아 보정 부호도 반대로 적용했다. 사용자에게 고정되어 보여야 하는 것은 target header의 viewport 위치라는 기준을 놓친 것이 핵심 실패였다.

### 재감사 범위

재감사는 잘라낸 규칙과 관련된 새 사고가 발생했거나 사용자가 명시적으로 요청한 경우에만 실행한다. 날짜와 과거 감사 기록은 검토 필요성을 알리는 참고사항일 뿐 세션 시작 자동 실행 조건이 아니다.

재감사를 실행할 때는 기록으로 다음을 확인한다.

1. `docs/_archive_claude_rules.md`의 11건 중 사고 때문에 되살릴 것이 있는가
2. 조건부 유지 항목이 생겼거나 실제로 발동했는가
3. 남은 규칙 중 한 번도 적용되지 않은 것이 있는가 — 단 대화 전용 규칙은 커밋 횟수로 판정하지 않는다
4. 다른 SSOT로 이동한 규칙의 내용이 실제 작업과 커밋에 나타나는가

상주 규칙이 실제 사고를 막았으면 가능할 때 해당 작업 커밋 메시지에 “어떤 규칙이 무엇을 막았는지” 한 줄로 남긴다. 다만 이 기록 요구가 원래 작업의 완료나 올바른 커밋을 방해해서는 안 된다.
