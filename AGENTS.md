# AGENTS.md

Codex가 이 프로젝트에서 따라야 할 규칙.

---

## 세션 시작 시 필독

모든 작업 세션 시작 전에 반드시 아래 두 문서를 먼저 읽는다.

- `docs/PROJECT_STRUCTURE.md` — 페이지 구조, JS 역할, 데이터 흐름 (인덱스)
- `docs/PROJECT_STATE.md` — 현재 완료 기능, 버그 목록, 추후 작업

세션 시작 시 및 **작업 주제가 바뀔 때마다** 아래 표를 보고 해당 파일을 읽는다. **해당 파일을 읽기 전에 수정하지 않는다.**

`PROJECT_STATE.md §0(진행 중 작업)`에 문서가 명시된 경우, 세션 시작 시 해당 문서를 함께 읽는다.

§0에 진행 중 항목이 있는 경우, 해당 문서를 읽은 뒤 아래 형식으로 복원 보고를 한다:
- 현재 목표
- 보류 항목 (사유 포함)
- 미실행 항목
- 다음 작업 후보

사용자가 별도 정정하지 않으면 복원 보고를 현재 상태로 간주하고 진행한다.

| 작업 종류 | 필독 파일 |
|-----------|----------|
| DB 테이블/컬럼 추가·변경 | `docs/db-schema.md` |
| CottageDB 함수 / window 전역 API | `docs/js-api.md` (크로스파일 의존관계 포함) |
| localStorage / 세션 키 | `docs/ls-schema.md` |
| 인증·로그인·프로필·사진 흐름 | `docs/PROJECT_STRUCTURE.md` §3~5 |
| 이용시간·체류 추적 | `docs/PROJECT_STRUCTURE.md` §6 |
| 게임 데이터·파이프라인 | `docs/PROJECT_STRUCTURE.md` §7~8 |
| BGG ID 수정 / fetch:bgg / translate 등 파이프라인 작업 | 작업 전 `npm run check`로 필요 단계 먼저 확인 |
| UI/CSS 디자인 작업 | `docs/DESIGN_RULES.md` |
| 업적/칭호/캐릭터/교환권/성장보드 | `docs/achievement-system.md` (SSOT) |

---

## 문서 관리

- 추후 작업 목록은 PROJECT_STATE.md에서만 관리한다. TODO.md는 사용하지 않는다.
- docs/TODO.md가 있으면 삭제한다.
- 새로운 `*.md` 문서를 생성하기 전에 기존 문서에 흡수 가능한지 먼저 검토한다.
- 일회성 작업 기록은 PROJECT_STATE.md 또는 작업 완료 후 삭제를 우선 검토한다.
- 장기적으로 독립 관리가 필요한 경우에만 신규 문서를 생성한다.

복원 비용이 높다고 판단되면 `*_CHECKPOINT.md` 생성을 제안하고, 승인 후 PROJECT_STATE.md §0에 등록한다.

복원 비용이 높아지는 대표 신호:
- 보류된 결정이 생겼을 때
- 후보안이 여러 개일 때
- 여러 페이지/파일을 비교 분석했을 때
- 대화에서만 존재하는 판단 근거가 있을 때
- 장시간 판단 작업이 이어지고 있을 때

체크포인트는 작업 종류(CSS/버그/감사)가 아니라 복원 비용으로 판단한다.

- 서브파일 갱신은 **코드 변경과 같은 커밋에 포함**시킨다. "나중에 한꺼번에" 금지 — compact 시 날아감.
- 커밋 순서:
  1. 코드 변경
  2. 해당 서브파일 즉시 갱신 (아래 트리거 테이블 참조)
  3. PROJECT_STATE.md — 변경사항 반영
  4. git diff 확인 후 커밋 (코드 + 서브파일 + STATE.md 한 커밋)
- 서브에이전트에 작업을 위임한 경우, 완료 후 문서 갱신 여부를 직접 확인한다.
  서브에이전트는 AGENTS.md 규칙을 따르지 않는다.

### 작업기록 동기화 실패 방지

코드와 문서의 상태가 어긋나면 이미 해결된 문제가 다시 미해결처럼 보인다. 따라서 문제를 고칠 때는 원인이 다시 생기지 않도록 아래를 함께 처리한다.

- 버그, 리팩토링 후보, 체크포인트 항목을 해결하면 **원래 문제가 적힌 문서 위치**를 찾아 해결됨/보류/폐기 중 하나로 닫는다.
- `REFACTOR_CHECKPOINT.md`, `PLAN_*.md`, `PROJECT_STATE.md` 중 같은 항목을 언급한 문서가 여러 개면 서로 같은 상태가 되게 맞춘다.
- 코드 수정 없이 재검증만 해서 "문제 없음"으로 판단한 경우도 문서에 재검증 근거를 남긴다. 예: "현재 함수 없음", "정의 1곳", "호출처 없음".
- 완료 기록에는 결론만 쓰지 말고 **왜 다시 문제가 아니라고 봐도 되는지**를 짧게 쓴다.
- 커밋 전 `rg`로 항목 ID나 핵심 함수명(`SC2`, `CSS1`, `renderSingleGame` 등)을 검색해 낡은 미해결 기록이 남아 있는지 확인한다.
- 이미 해결된 항목이 문서에 미해결처럼 남아 있으면 코드 수정이 아니라 문서 정합성 문제로 분류한다.
- "완료"라고 말하려면 코드 변경 여부와 문서 상태가 함께 닫혔는지 확인한다.

## 외부 의견 수렴 원칙

사용자가 GPT, Codex, Gemini 등 외부 AI 또는 제3자 의견을 가져올 경우:
- 해당 의견을 즉시 정답으로 간주하지 않는다.
- 현재 프로젝트의 코드, 문서, DB 구조를 기준으로 재검증한다.
- 외부 의견과 현재 구조가 다를 경우 차이점을 먼저 보고하고 사용자 승인을 받는다.
- 승인 없이 외부 의견을 그대로 구현하지 않는다.

사용자가 출처를 명시하지 않아도 문체 패턴(긴 구조화 목록, 설명체, 프로젝트 문맥 없는 일반론)으로 외부 AI 출력임을 파악하고 동일하게 재검증한다.

외부 의견은 구현 지시가 아니라 감사 입력으로 취급한다. 최종 판단 기준은 항상 현재 프로젝트의 실제 상태이다.

## 운영 규칙 관리

작업 중 운영 방식이 바뀌면:
- 이번 세션만 적용 → PROJECT_STATE.md에 메모
- 앞으로 계속 적용 → AGENTS.md 갱신 대상. 작업 권한 Yellow 절차에 따라 변경안 보고 후 승인.

커밋 전, 바뀐 운영 방식이 있으면 사용자에게 한 줄로만 물어봐라.  
예: "TODO.md 삭제 규칙 AGENTS.md에 추가할까요?"  
목록 나열하거나 길게 설명하지 말 것.

## Plan-Execute 기준

아래 중 하나라도 해당하면 Plan을 먼저 작성하고 승인 후 실행한다:

| 조건 | 이유 |
|------|------|
| DB 테이블/컬럼 신규 생성 또는 변경 | 되돌리기 어려움, 다른 파일 영향 큼 |
| localStorage 키/구조 신규 생성 또는 변경 | 기존 키 재사용 가능한지 먼저 확인 필요 |
| window.CottageDB 공개 API 추가/변경/삭제 | 다수 파일이 의존, 파급 범위 큼 |
| 인덱스(STRUCTURE.md) 및 서브파일 확인 후에도 영향 범위 불명확 | 읽지 않고 실행하면 중복/충돌 위험 |

### Plan 작성 형식

```
읽을 파일: [파일명:줄번호 범위]
변경할 대상: 기존 함수/키/테이블 X → Y (재사용 or 신규, 신규면 이유)
새로 생성: 없음 / [생성 대상 + 이유]
영향 파일: A.js, B.html (호출하거나 의존하는 파일)
위험요소: 오탐/충돌/예외 케이스 + 첫 번째로 실패할 가능성이 높은 지점
```

## 신규 생성 원칙

리팩토링의 목적은 분리가 아니라 규모에 맞는 구조 최적화다. 유지·통합·분리·삭제 중 가장 단순한 해결책을 우선 검토한다.

새 localStorage 키, DB 테이블, 전역 함수, CSS 클래스를 만들기 전에:

1. `docs/ls-schema.md`, `docs/db-schema.md`, `docs/js-api.md`에서 동일 용도 기존 항목 검색
2. 기존 것을 확장(컬럼 추가, 필드 추가)할 수 있으면 그 방향으로 한다
3. 신규 생성이 불가피할 경우 이유를 Plan 또는 커밋 메시지에 명시한다

## 문서 갱신 트리거

| 변경 사항 | 갱신할 파일 |
|-----------|------------|
| DB 테이블/컬럼 추가·변경 | `docs/db-schema.md` |
| CottageDB 함수 추가·제거·시그니처 변경, 또는 window.xxx 전역 변수 추가 | `docs/js-api.md` |
| localStorage 키/구조 변경 | `docs/ls-schema.md` |
| 페이지 파일 이동/추가/삭제 | `docs/PROJECT_STRUCTURE.md` |
| 인증·데이터 흐름 변경 | `docs/PROJECT_STRUCTURE.md` |
| 기능 완료·버그 발견·작업 추가 | `docs/PROJECT_STATE.md` |

## 구현/리팩토링 분리 원칙

기능 구현과 리팩토링은 같은 작업 단위에서 섞지 않는다.

### 구현 단계
- 먼저 동작하는 기능을 완성한다.
- 완벽한 구조보다 기능 완성이 우선이다.
- 단, 이후 리팩토링이 어렵지 않도록 다음은 지킨다:
  - 역할이 명확한 헬퍼 함수는 추출한다.
  - 1회성 로직은 과도하게 분리하지 않는다.
  - 변수명과 패턴은 기존 코드 스타일과 맞춘다.
  - 중복 로직을 발견하면 공통화 가능성을 확인하되, 즉시 대규모 정리는 하지 않는다.
- 구현 중 발견한 리팩토링 후보는 PROJECT_STATE.md §0 `코드 품질 주석`에 메모하고 넘어간다.

### 리팩토링 단계
- 기능 테스트 완료 후 별도 세션에서 진행한다.
- REFACTOR MODE 규칙을 따른다.
- 신규 기능, UI 변경, 정책 변경을 함께 하지 않는다.

## 작업 모드 분리

작업 시작 전 완료 조건을 한 문장으로 정의한다.  
예: "취향보드 게임 카드 클릭 시 바텀시트가 열린다." — 이 조건이 만족되면 완료.

### BUG FIX MODE
버그 수정 중 아래를 금지한다.
- 리팩토링, 구조 개선, 파일/함수 분리, 명명 변경, CSS 정리
- 다른 문제 수정 (발견하면 보고만 하고 넘어간다)
- **git revert 금지** — 이전 커밋을 되돌려야 할 필요가 생기면 즉시 멈추고 사용자에게 이유를 먼저 설명한다. 승인 없이 revert하지 않는다.

### REFACTOR MODE
리팩토링 중 아래를 금지한다.
- 신규 기능 추가, UI 변경, 요구사항 변경

## WIP 제한

활성 작업(WIP)은 동시에 3개를 초과하지 않는다.  
작업 중 떠오른 새 아이디어는 PROJECT_STATE.md 다음 작업 후보에 기록만 한다. 즉시 구현하지 않는다.

## 버그 수정 방식

- 버그를 받으면 증상 → 관련 함수 → 파일 순으로 가설을 먼저 텍스트로 정리한다.
- 읽을 파일을 1~2개로 한정한 뒤 Read한다. "관련될 수 있는 모든 파일" 전체 탐색 금지.
- 방문자·통계 데이터 관련 버그 수정 시 데이터 초기화 금지. 원래 있어야 할 값으로 복원하는 방식으로만 수정한다.

### 파일 읽기 전 경로 확인 원칙

소스 파일을 Read하기 전에 반드시 아래 순서로 위치를 먼저 확인한다:

1. `docs/PROJECT_STRUCTURE.md` — 파일 역할과 경로 확인
2. 해당 작업 종류의 docs (`js-api.md`, `db-schema.md` 등) — 함수명·줄번호 확인
3. 필요한 줄 범위만 Read. 파일 전체 읽기 금지.

Grep으로 줄번호를 먼저 확인한 뒤 해당 줄 ±30줄 범위만 Read한다.

### 반복 패치 정지 규칙

같은 파일·함수·CSS 선택자를 **2회 이상** 수정해도 해결이 안 되면 즉시 작업을 멈추고 사용자에게 보고한다.  
"2번 수정했는데 아직 해결이 안 됩니다 — 접근 방향이 틀렸을 수 있습니다. 다른 방향을 검토합니다."  
반복 패치는 근본 원인이 아닌 증상만 건드리고 있다는 신호다.

**3회차부터는 코드 수정 금지.** 다음을 먼저 보고한다:
- 원인 가설
- 검증 방법
- 변경 예정 파일
- 롤백 방법

사용자 승인 전에는 파일을 수정하지 않는다.

**완료 기준**: "수정 완료"라고 말하려면 실제 재현 확인 결과를 함께 제시한다. 재현 확인을 못 했으면 "완료"라고 말하지 않는다.

**브라우저/CSS 렌더링 버그**: 값만 바꾸지 말고 구조 분리 가능성을 먼저 검토한다.

## 커밋 전 검증 (필수)

"될 것 같다"는 느낌으로 커밋하지 않는다. 아래 중 하나 이상으로 실제 동작을 확인한 뒤 커밋한다.

- **로직 추적**: 변경한 코드를 실제 데이터 값으로 한 단계씩 따라간다 (특히 날짜·시간 계산, DB 쓰기, 누적·집계)
- **node 테스트**: 서버사이드 로직이면 `node` 스크립트로 실행해 출력 확인
- **엣지케이스 체크**: 0값, null, 날짜 경계, SELECT 실패 등 예외 경로 명시적으로 확인
- **UI/CSS 버그는 브라우저 직접 확인 필수** — "될 것 같다"로 CSS 커밋하지 않는다. 시각적 버그는 눈으로 확인하기 전까지 완료가 아니다.

같은 버그를 반복 수정하는 패턴은 근본 원인이 아닌 증상만 건드린 것이다.  
확인이 불충분하면 커밋을 보류하고 추가 분석을 먼저 한다.

**Plan이 필요했던 작업(신규 기능·API·상태 변경)은 커밋 전 사용자 시점으로 한 번 더 훑는다.**  
목적은 코드 정합성이 아니라 의도와 실제 동작의 불일치 확인이다 (예: 배지가 숨겨진 버튼에 달려 있음, 패널을 열었더니 알림이 사라짐 등).  
구현자 시점("어떻게 만들까")과 사용자 시점("어디서 깨질까")은 다르다.

## 영구 식별자(Immutable ID) 원칙

다음 ID는 사용자 데이터가 누적되는 기준이므로 배포 후 변경하지 않는다.

- `achievements.id` (예: `record_1`, `new_game_20`, `review_100`)
- `game_play_records.game_id` (BGG ID)

이 ID들은 도감 완성률, 새 게임 경험 수, 캐릭터 해금, 플레이 통계의 기준이다.  
**이 시점부터 사용자 자산(획득 캐릭터, 도감 진행률, 포인트)이 존재한다. DB 변경 전 "기존 사용자가 무엇을 잃는가?"를 먼저 확인한다.**

변경 가능: 게임명, 설명, 이미지, 업적 이름, 업적 포인트  
변경 불가: ID 자체

| ❌ 금지 | ✅ 허용 |
|--------|--------|
| `rabbit_20` → `rabbit_explorer_20` | `name` 필드만 수정 |
| BGG ID 정정으로 기존 플레이 기록 game_id 변경 | 신규 게임에 새 BGG ID 사용 |

ID 변경이 불가피한 경우: 기존 ID 유지 + 별도 migration 계획 작성 + 기존 사용자 데이터 이전 방안 설계 후 승인 필요.

---

## 작업 권한

**Green** — 사용자 승인 없이 진행 가능  
CSS 값 변경 (color, padding, margin, font-size 등 수치·값만), 문구·오타 수정, 배너 교체, PROJECT_STATE.md 사실 기록 갱신 (완료 반영, 버그 이력 등)

**CSS 구조 변경은 Yellow**: 선택자 스코프 변경, 클래스 추가·제거, `position`·`z-index`·`display` 변경, 선택자 신규 추가·삭제 → 변경안 보고 후 승인 필요.

**Yellow** — 변경안 보고 후 승인 필요  
AGENTS.md 수정, DESIGN_RULES.md 수정, 신규 `*.md` 생성, PROJECT_STATE.md 구조 변경 (섹션 추가·삭제, 문서 체계 변경)

**Red** — Plan 후 승인 필요 (Plan 형식: Plan-Execute 기준 참조)  
DB, 인증, 데이터 구조, 파일 삭제, 대규모 리팩토링

### 작업 완료 보고

진행 중 작업이 여러 개이거나 미실행 항목이 있는 경우, 커밋 후 다음을 구분해서 보고한다:
1. 이번에 변경된 파일/내용
2. 미실행 항목
3. 다음 후보 작업

## Git 작업

- 작업이 완료될 때마다 자동으로 커밋한다.
- commit 후 자동으로 push하지 않는다. push는 사용자가 명시적으로 요청할 때만 한다.

---

## CSS/sticky 버그 교훈 (2026-07-03)

아래 유형은 단순 수치 조정처럼 보여도 실제 원인은 레이아웃 구조인 경우가 많다.

- sticky / scroll / bottom sheet / fixed header / iframe sheet / border-radius / overflow

### 원칙

1. **사용자가 말한 기준 컴포넌트를 다시 확인한다.**
   - "게임정보 시트와 동일"과 "게임위치 시트와 동일"은 다르다.
   - 최근에 수정한 값을 기준으로 삼지 말고, 현재 요청의 비교 대상을 다시 확인한다.

2. **공백이 스크롤 영역인지 고정 영역인지 먼저 구분한다.**
   - 기록보드 버그의 핵심은 `padding-top` 값이 아니라, 공백이 scroll body 안에 있어 내용이 그 틈으로 지나간 것이었다.
   - `헤더1 + 공백 + 헤더2`가 함께 고정돼야 하면 공백도 sticky/fixed 영역에 포함되어야 한다.

3. **패널 시작 위치, height, sticky top을 건드리지 말라는 요청은 그대로 지킨다.**
   - 그런 요청에서는 header padding/margin 같은 "고정되는 영역 자체"만 줄인다.
   - 패널 위치를 내리거나 height를 바꾸면 겉으로 비슷해 보여도 다른 버그를 만든다.

4. **같은 선택자를 2번 이상 건드려도 안 되면 값 추측을 멈춘다.**
   - DevTools 또는 런타임 값으로 `getBoundingClientRect()`, `getComputedStyle()`, `offsetHeight`, `clientHeight`를 확인한다.
   - `calc()`, `var()`, `env()`가 섞인 CSS 변수는 `parseFloat()`로 숫자화하지 않는다. 실제 DOM 크기를 재거나 CSS가 처리하게 둔다.

5. **diff를 보고 의도 밖 선택자가 바뀌었는지 확인한다.**
   - 2026-07-03 버그5 작업 중 `sheet-sticky-bar`가 아니라 `hero-visitor-bar` padding이 먼저 잡힌 적이 있었다.
   - 커밋 전 diff에서 대상 선택자 이름까지 확인한다.

### 이번 사례별 메모

- 버그1: 내 보드 고정헤더 영역 과대. 원인은 header 자체 padding + 아래 margin이 함께 sticky 영역처럼 보인 것. 패널 top/height는 건드리지 않고 `.profile-panel-header` padding/margin만 줄였다.
- 버그2: 기록보드 헤더 사이 공백으로 본문이 지나감. 원인은 공백을 scroll padding으로 만든 것. `.profile-subsheet-body--records::before` sticky 덮개로 공백을 고정 영역에 포함했다.
- 버그3: 게임위치 바텀시트 상단 여백. 원인은 `height: calc(100dvh - Npx)`가 상단 여백을 결정하는 구조. 좌우/아래/radius/overflow는 유지하고 height 값만 조정했다.
- 버그4: 홈페이지 기능 iframe 시트 여백. 실패 원인은 게임정보/내 보드 기준이어야 하는데 직전 게임위치 시트의 `102px` 기준을 복사한 것. 실제 기준은 `height: calc(100dvh - 48px)` + 아래 margin 12px이었다.
- 버그5: 고정헤더 높이 축소. 위치 구조 변경 없이 padding/min-height 값만 줄였다. diff 확인 중 의도 밖 선택자 변경을 발견해 커밋 전 원복했다.
