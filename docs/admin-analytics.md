# admin-analytics.md — 관리자 분석 도메인 SSOT

최종 갱신: 2026-07-20 (#25·#3·#26·교환권 도배 종결 + #18 판정종결(고칠 것 없음) / 「시간」 계열 #19·#22·#24·R3(#12·#20·#21·#23) + P5 청소)

> ✅ **관리자 화면의 「시간」은 닫혔다.** 값이 새던 것(#22)·3배 부풀던 것(#24)·화면에서 갈리던 것(R3)이 모두 해소됐고, 화면 값이 **DB 직접 집계와 초 단위로 일치**한다.
>
> 🎯 **다음은 P3b(드릴다운)** — 원칙 ⑤의 최대 결손이자 "유기적"의 핵심. 이제 소스가 하나라 내려가도 숫자가 안 갈린다.
>
> 📌 **남은 열림은 #14 하나**(🟡, Plan 필요 — 저장 경로 2개 통일 + 과거 행 소급). 목표를 닫는 건 **P3b**다.
>
> 🚨 **대장에 적힌 「0건」·「발현 없음」을 근거로 쓰지 말 것.** 2026-07-20에 #18(「DB 0건」→실제 5건)과 #3(「발현 0건」→실제 13세션)이 **연달아 뒤집혔다.** 착수 첫 동작은 **그 숫자를 다시 재는 것**이다(§4 원리 4).
>
> 📖 **읽는 순서**: §1 원칙 → §4 「원리」 14개 → §5 계획. **종결 표와 §5-1 제약은 필요할 때만.**

**대상 화면**: `pages/admin/requests-admin.html`의 **분석 영역**(요약/방문/유입/페이지/이벤트/회원 6탭).
요청관리(게임·간식·건의) 영역은 이 문서 범위 밖이며 **아직 감사도 안 됐다**.

> **왜 이 문서가 생겼나**: 같은 주제가 PROJECT_STATE §3 3곳 + 「론칭 후 이월」 + `PLAN_funnel_analytics.md`로 흩어져, 다음 세션에 무엇부터 볼지 판단이 안 되는 상태였다(2026-07-19 사용자 지적: "펼쳐진 내용들이 많으니 전체 정리 및 통합 정리"). `achievement-system.md`가 업적 도메인의 SSOT인 것과 같은 자리다.

---

## 1. 원칙

2026-07-19 사용자 요청("유효 데이터들이 더 직접적이고 유기적으로 드러났으면. 페이지별로도, 페이지 내 항목별로도")에서 도출. **근거는 관념이 아니라 이 화면에서 실제로 터진 발견 #1~#14다** — 그 14건 중 "지표가 부족하다"는 하나도 없었고 **전부 "이 숫자가 무엇을 센 것인가"의 문제**였다.

⚠️ **이건 시각 디자인 규칙이 아니다.** `DESIGN_RULES.md`(팔레트·모바일·`border-radius`·sticky)와 범주가 다르다. ①②는 **데이터 정의**, ③④⑤는 **정보 구조(IA)**다.

### ① 모든 수치는 "무엇을·언제·누구를" 셌는지 달고 다닌다

단위(회/명)·기간·모집단을 표기하거나, 표기가 지저분하면 **화면 전체가 하나의 기준을 따르게** 한다.

- 위반 실례: **#9**(요약 카드 5장의 기간이 오늘/7일/30일/누적으로 제각각인데 1장만 표기) · **#11**("7일" 버튼이 실제로는 이번 주) · **#5**(필터 칩은 누적 341, 목록은 그날 4명)
- **판정법**: 화면의 숫자 하나를 짚고 "이건 언제, 누구를 센 거냐"에 답할 수 없으면 위반.

### ② 절대수보다 분모 붙은 수 — 단, 분모는 같은 기간·같은 모집단

"23명"은 많은지 적은지 알 수 없다. "그 기간 방문 249명 중 23명(9%)"이면 즉시 판단된다.

- ⚠️ **분모를 붙이기 전에 분자가 분모에 포함되는지 실측**한다. 2026-07-19 도달률 설계 시 `page_events`의 고유 `session_key` 70개가 `page_sessions`에도 **100% 존재**함을 확인하고서야 지표를 인정했다. 교집합이 100%가 아니면 그 비율은 거짓말이 된다.
- ⚠️ **기간을 맞춘다.** 전체 방문자 763명이 아니라 **이벤트가 존재하는 기간의 249명**이 분모다.
- ⚠️ **섞으면 안 되는 모집단은 나눈다.** 도달률이 회원 88%(16명 중 14명) vs 비회원 24%(233명 중 56명)로 갈린다 — 합친 28%는 두 이야기를 다 지운다.

### ③ 파생 가능한 값은 그 자리에서 반복하지 않는다

`평균 = 회 ÷ 명`은 이미 있는 두 수에서 나온다. 좁은 카드에 셋을 넣으면 셋 다 안 읽힌다.

- **단, 비교가 목적인 자리에선 파생값이 1급이 된다** — 이벤트 탭의 단계별 평균은 단계 간 몰입도 비교라 유지한다.
- **판단 기준**: "이 수가 없으면 못 하는 판단이 있는가?" 없으면 빼라.
- 적용 실례: 2026-07-19 "요약 카드에도 평균을 넣을까?"는 이 원칙으로 **기각**, 같은 날 "도달률을 넣을까?"는 ②에 해당해 **채택**.

### ④ 한 화면은 한 질문에 답한다

요약=**어디가** 활발한가 / 방문=**언제** 오나 / 유입=**어디서** 오나 / 페이지=**무엇을 보나** / 이벤트=**무엇을 하나** / 회원=**누가**.

- 위반 실례: **#12**(회원 탭에 「회원별 이용시간」이라는 *기간 집계* 차트가 있다 — 방문 탭 소관)
- 날짜 네비가 어느 탭에 붙느냐도 이 원칙으로 결정된다(**#5·#6**: 회원=명부라 기간 무관, 이벤트=기간별 건수가 본질).

### ⑤ 지표에서 그것을 구성하는 다음 단위로 내려갈 수 있다 (드릴다운)

**"유기적"의 정체가 이것이다.** 한 숫자를 보고 "이걸 만든 게 누구/무엇이지?"로 내려가는 길이 있어야 한다.

- 있음: 요약 카드 → 해당 탭(`data-nav-tab`) · 회원 카드 → 그 사람의 페이지 분포(2026-07-19 B단계)
- **없음(최대 결손)**: 이벤트 계열 → 그 계열의 단계 / 페이지·이벤트 → **그걸 한 사람들** / 페이지 → 그 페이지를 본 회원 목록
- 지금은 **각 탭이 섬**이라 "이 숫자를 만든 사람이 누구지?"에서 막힌다. 하나씩 고쳐서는 안 메워지는 종류의 결손.

---

## 2. 화면 구조

| 탭 | 답하는 질문 | 날짜 네비 | 비고 |
|---|---|---|---|
| 요약 | 어디가 활발한가 | ✅ | 카드 5장, 클릭 시 해당 탭으로(`data-nav-tab`) |
| 방문 | 언제 오나 | ✅ | 날짜별 차트 + 서브탭(시간대/회원·비회원). **요일·재방문율은 `disabled title="준비 중"`** = 2단계가 붙을 예약석 |
| 유입 | 어디서 오나 | ✅ | 유입경로별 + 유입×페이지 교차 |
| 페이지 | 무엇을 보나 | ✅ | 가로막대. **막대=방문 수**(사람·페이지·날짜를 접은 값), **막대 위 `⏱` 텍스트=체류** — 서로 다른 지표라 축 제목으로 구분 |
| 이벤트 | 무엇을 하나 | ✅ | **퍼널이 아니라 「행동 묶음 + 겹침」**(#26 — 포함·순서를 데이터가 보장하지 않는다). 비율은 사람 집합 교집합 |
| 회원 | 누가 | ❌(의도) | 명부. 카드마다 `📄 페이지 N개` 펼침 |

**이벤트 계열 표(`EVENT_FAMILIES`)가 단일 출처다.** 조회 목록(`EVENT_ALL_TYPES`)이 여기서 파생되므로 **새 `trackEvent` 타입은 반드시 여기 등록**한다. 안 하면 관리자 화면이 아예 조회하지 않아 조용히 안 보인다(그래서 실제로 77%가 안 보였다 = **#13**).

---

## 3. 데이터 소스와 함정 (읽기 전 필독)

| 소스 | 용도 | 함정 |
|---|---|---|
| `page_sessions` | 방문·체류·페이지 분포 | 11,700행+. `duration_sec=0`은 **전부 비회원**(회원 10,504행 중 0건) — 이탈이 아니라 하드코딩된 설계값. §4 ① 참조. ⚠️ heartbeat 분이 안 들어와 **체류의 하한**이다(#22) |
| `page_views` | `__visitor__` 마커 = 방문 명/회 | legacy 행 70%가 `session_key` NULL(소급 불가) |
| `page_events` | 이벤트·퍼널 | `session_key` **2026-07부터 100%, 06월은 8%** |
| `anon_sessions` | 비로그인 heartbeat | `today_date`가 세션 시작값 고정(자정 넘김 버그) |

- 🚨 **관리자(`4916417947`)는 트래킹에서 영구 제외**된다(`_shouldSkipAnalytics`). **이벤트 기반 지표는 관리자 활동을 절대 반영하지 않는다** — "왜 내 활동이 없지?"가 아니라 설계된 동작.
- 🚨 **PostgREST `max-rows`는 대시보드 설정이라 마이그레이션에 안 남는다.** 현재 50000(2026-07-18 상향). DB 재구축 시 1000으로 돌아가 **조용히 91% 절단이 재발**한다. 상세는 [db-schema.md](db-schema.md) "PostgREST max-rows".
- 🚨 **`page_sessions.page`는 슬러그가 아니라 표시 라벨을 저장한다**(#14) — 라벨을 바꾸면 과거 데이터가 쪼개진다.
- 🚨 **"방문자 N명"의 소스가 둘이고 서로 호환되지 않는다** (2026-07-19 P1에서 실측 확정):
  - **비율의 분모로는 `page_sessions`만 쓴다.** 이벤트를 일으킨 회원 14명 중 `__visitor__`에 있는 사람은 **9명(64%)**뿐 — 분자가 분모 밖으로 새서 **100% 초과가 나온다**(#10과 같은 종류의 거짓말). `page_sessions`는 회원·비회원 모두 **100% 포함**(7일/30일/전체 전부).
  - `__visitor__`(방문 탭 차트·유입·도넛)와 `page_sessions`(도달률)는 **숫자가 다른 게 정상**이다. 원칙 ①에 따라 **어느 기준인지 화면에 표기**해서 공존시킨다(도달률 줄의 "방문=세션 기록 기준").
  - 두 소스를 통일하는 건 방문 탭 전체가 `__visitor__` 기준이라 **Red 규모** — 지금 손대지 말 것.
- **행 수 자체가 거짓말한다**: 정확히 `1000`이면 절단, `0`이면 RLS를 의심. 둘 다 `error`가 `null`이라 감지기에 안 걸린다. 상세는 CLAUDE.md 「DB 함수 에러 처리」.

**검증 하니스**: `scripts/audit-admin-analytics.js`(DB층 — 절단/RLS) · `scripts/shot-admin-tabs.js`(표시층 — 6탭 스크린샷 + #5·#6·#7 자동 판정, 전환율 100% 초과 회귀 가드).
⚠️ **하니스 출력 폴더를 리포 안으로 주지 말 것** — Live Server가 리포를 감시해 PNG 쓰기가 페이지 리로드를 유발하고, 두 번째 탭부터 죽는다(원인 규명에 2회 실패를 소모했다).

---

## 4. 발견 대장

### ✅ 종결

| # | 내용 | 해결 |
|---|---|---|
| 1 | `page_events` SELECT가 `authenticated`로 막힘 → 1,452행 전부 안 보임 | 마이그레이션 011 (2026-07-18) |
| 2 | `max-rows` 1000 절단 → 데이터의 9%만 봄 | 50000 상향. 총 체류 12시간→276시간(23배) |
| 5 | 필터 칩(누적 341) vs 목록(그날 4명) 모집단 불일치 | 회원 탭을 날짜 네비에서 분리 (2026-07-19) |
| 6 | 퍼널 전환율이 오늘 표본만 써서 5칸 전부 `-` | 퍼널을 날짜 네비에 연결 (2026-07-19) |
| 10 | 전환율이 100%를 넘음(167%·118%) | **사람 집합 교집합**으로 계산 → 원리상 불가 (A단계 부산물) |
| 13 | 이벤트 1,493행 중 1,149행(77%)을 조회조차 안 함 | `EVENT_FAMILIES` 단일 출처화 (2026-07-19) |
| 15 | **방문자 도넛·요약카드가 사람 아닌 행을 셈 + 회원 재방문이 비회원으로 오염** | 공용 헬퍼 `visitorKey` 사용으로 통일 (2026-07-19) |
| 8 | 막대 길이(방문 수)와 막대 위 텍스트(체류)가 다른 지표인데 축에 설명 없음 | x축 제목 + `⏱` 접두사 + 넘칠 때 막대 밖 표기 (2026-07-19) |
| 9 | 요약 카드 기간 기준 제각각 + 「주요 유입」 말줄임 | 라벨을 실제 기간에서 파생 + 체류시간 제거(모집단이 달랐다) (2026-07-19) |
| 11 | "7일" 버튼이 실제로는 이번 주, "30일"은 이번 달 | 버튼 라벨을 단위 선택기 이름으로 정정 (2026-07-19) |
| 16 | `getPageAnalytics` 90일 컷으로 '전체'가 조용히 거짓이 됨 | 경계 도달을 실행 시점에 감지해 "전체 (최근 90일)"로 자동 전환 (2026-07-19) |
| 19 | `record_complete`가 저장 경로 6곳 중 1곳에서만 발화(231 대 1) | `game-sheet.js` 5곳에 `trackEvent('record_complete', {game_id})` 추가 (2026-07-19) |
| 24 | `index.html` 프레임 3개가 각자 세션 추적을 돌려 같은 실시간이 3배 계상 | `_isEmbeddedFrame()` 가드로 세션·방문 추적을 최상위 프레임만 (2026-07-19). E2E **3.50배 → 0.88배**, 유령 `page_views` 2건 → 0건. ⚠️ **부수 효과**: `guide.html`·게임위치 오버레이처럼 **사용자가 실제로 보는 iframe의 `page_views`도 이제 안 잡힌다**(부모 조회에 흡수) — 그 페이지별 유입이 다시 필요하면 별도 설계가 필요하다 |
| 22 | `profiles` 카운터가 비원자적 read-modify-write라 동시 flush에서 증가분 손실(탭 2개면 33%) | 원자적 증가 RPC (마이그레이션 012, 2026-07-19). 통제 실험으로 동시성 2·5·30 전부 **손실 0** 확인 |
| 3 | 비회원 카드의 「오늘」이 `page_sessions` 합(0)에 덮여 사라짐 — 대장엔 「현재 발현 0건」으로 적혀 있었다 | 🚨 **발현하고 있었다** (2026-07-20 실측: 세션 326개 중 **13개**에서 값이 커짐). 원인은 **두 소스 중 하나만 골라 쓴 것** — 트래커 경로는 3초 미만·flush 실패분이 빠지고 heartbeat 경로는 60초 단위라 **둘 다 하한**이다. `Math.max(page_sessions 오늘, anon_sessions.today_seconds)`로 변경 + 출처 툴팁. 판정식 8케이스 + 실데이터 326세션 불변식(새 값 ≥ 옛 값) 통과, 관리자 화면 육안 확인 |
| 18 | **`home_record_write_click`이 DB 0건** — 퍼널 3단계가 늘 비어 있었다 | 🚨 **고칠 게 없었다 — 전제가 낡은 것이었다** (2026-07-20 실측). 실제로는 **5건**이 쌓여 있고(2026-07-19~, 회원 1명 4건 + 비회원 1건) 그중 두 건은 뒤이어 `record_complete`까지 이어진 **진짜 사용자 흐름**이다. 후보 4개 중 ③(실서버 배포본 상이)은 `cottageboard.com` 실파일에 코드 존재로, ④(리스너 미부착)는 Playwright 실클릭으로, ②(관리자 전용)는 비관리자 행 존재로 각각 탈락 → **추적 결손이 아니다. 클릭 자체가 적은 것이다.** ⚠️ **왜 적은지는 아직 모른다** — 방문 모수·버튼 노출·다른 진입 경로·07-19 이전 활성 회원 부재가 전부 후보로 남아 있다. **"그냥 안 눌린다"로 단정하지 말 것**(2026-07-20에 그렇게 적었다가 정정). 사용량 분석이 필요해지면 **추적 버그가 아니라 UX·퍼널 항목으로** 따로 세울 것 |
| 교환권 도배 | 관리자 「최근 소식」 30건 중 대부분이 `voucher:*`라 실제 알림이 묻힘 | **의도된 사양은 맞았다**(관리자 전용 감시 피드). 문제는 비중이었고 **원인이 두 겹**이었다 (2026-07-20 실측): ①`dev_test` 발급 13건이 섞여 있었다 → 조회에서 제외 + 최근 30일 제한 ②그래도 19건이 남았는데 **그중 13건이 한 사람이 하루에 쓴 것** → **유형+날짜(KST)로 묶음**(`new_intro`와 같은 방식, 렌더러가 이미 `keys[]`를 지원). **19행 → 7줄**, 묶음 건수 합 = 원본 행 수 불변식 통과, 관리자 알림 육안 확인. 🚨 **`voucher_log` 행은 안 건드렸다** — 잔액·이력의 원장이고 데이터 삭제는 표시 문제의 해법이 아니다 |
| 26 | **이벤트 탭은 퍼널이 아닌데 퍼널로 그리고 있었다** — 화살표·트리가 「B를 한 사람은 A를 거쳤다」는 포함관계를 주장 | **도형을 없애고 「행동 묶음 + 겹침」으로 바꿨다** (2026-07-20). 각 행동을 나열하고, 관계는 문장으로만: `1-1 한 9명 중 1-2도 4명 44% · 1-3도 2명 22%` / `1-2·1-3 둘 다 한 사람 2명 (둘 중 하나라도 한 9명 중)`. 범례에 "퍼널이 아니다 · 순서도 거쳐야 하는 길도 세지 않는다" 명시. 계산식은 원래부터 무순서 교집합이라 **안 바꿨다** — 바꾼 건 **표현이 주장하는 것**이다. 육안 확인(오늘·전체), 콘솔 에러 0 |
| | 🚨 **판정 근거 — 포함관계가 5쌍 중 4쌍에서 깨진다**(2026-07-20 전 기간 실측): 1-1→1-2는 B 6명 중 **2명**이, 1-1→1-3은 5명 중 **3명**이, 2-1→2-2는 7명 중 **3명**이, 3-1→3-2는 23명 중 **6명**이 A를 하지 않았다. 홈 버튼들은 스크롤·딥링크 등 **여러 경로로 도달 가능**하다. **순서도 못 센다** — 세션·시각까지 비교하면 3-1→3-2는 17명이 아니라 **13명**(23% 차이). 진짜 퍼널이 필요하면 같은 `session_key` 안 A→B 시각 비교로 **새로 만들 것**. | |
| | 🚨 **이 자리를 하루에 세 번 고쳤고, 2·3차가 같은 실패였다** — ②`⇄` 기호 추가 ③진입 아래 트리. **둘 다 관계를 데이터로 확인하지 않고 그림부터 바꿨다.** 특히 ③은 외부 감사의 "선형으로 보인다"는 지적을 받고 **그 지적이 준 프레임(분기)을 그대로 해답으로** 삼은 것이다. **지적은 맞았고 내 해답이 틀렸다** — 내 화면 숫자가 이미 포함관계를 부정하고 있었는데 그걸 안 봤다. → CLAUDE.md 「외부 의견」에 판정법 추가. | |
| 25 | `record_start`가 홈 로드마다 자동 발사(기록 iframe 프리로드가 `tryInit()`을 돌림) — 요약 탭 「📋 플레이기록」 계열 카드의 분모가 부풀어 있었다 | 발사를 **사용자가 기록 입력 화면에 들어온 시점** 3곳으로 이동 (2026-07-20). 프리로드는 `embed=true`라 초기화 시 안 쏘고, 모달이 열릴 때 `cottage-switch-tab`으로 잡는다. Playwright 4경로 검증 + 음성 대조군(옛 구현에서 4건 FAIL). ⚠️ **소급 보정은 안 했다** — 과거 `record_start` 367건은 부푼 채로 남고 **앞으로 쌓이는 값만 정확**하다. 그 기간 비율을 인용할 땐 이걸 감안할 것 |
| **R3**(#12·#20·#21·#23) | **회원 탭 한 화면에 사람당 시간 숫자 4개·소스 3개** — 차트는 기간에 따라 지표 정의까지 바뀜 | **질문별 단일 출처**로 정리 (2026-07-20): 기간 있는 시간=`page_sessions` / 전체 누적=`profiles`, 각 자리에 출처 표기. 차트는 전 기간 고정(명부 탭이라 기간 선택이 없어 늘 비어 있었다). 화면 10명이 **DB와 초 단위 일치** |

#### 여기서 얻은 원리 (사례 번호는 위 표에서 상세 복원)

**진단**
1. **증상 수를 세지 말고 원인을 세라.** #5·#6은 한 원인(배선 반대), #9·#11도 한 원인(라벨 하드코딩), #12·#20·#21·#23도 **R3 하나**였다(2026-07-20 함께 종결). 증상 단위로 등록하면 대장만 길어지고 안 닫힌다.
2. **"그 숫자를 실제로 누가 읽는가"를 먼저 grep한다.** 원인부터 보면 고쳐도 화면이 안 변하거나 엉뚱한 데를 고친다(#19 — 퍼널이 `record_*`를 안 쓴다는 걸 착수 후에 알았다).
3. **관측으로 안 갈리면 통제 실험으로 넘어간다.** #22는 겹침시간↔손실 상관이 `r≈-0.03`으로 무의미했는데, **두 기전이 동시에 작동해 관측값이 둘의 합**이었기 때문이다. 임시 행에 동시 쓰기를 가하니 즉시 갈렸다.
4. 🚨 **"0건"은 발견이 아니라 질문이다 — 다시 세고 나서 착수한다.** #18은 **대장에 적힌 「DB 0건」이 낡은 값**이었고(실제 5건), 그걸 모른 채 "왜 0인가"의 원인 후보를 넷이나 세워두고 있었다. **없는 병의 원인을 찾고 있던 것.** 착수 첫 동작은 가설이 아니라 **그 숫자를 지금 다시 재는 것**이어야 한다.
5. **A/B가 책임 소재를 가른다.** 옛 코드로 되돌려 같은 측정을 해야 "내가 만든 것"과 "원래 있던 것"이 구분된다(#24 — 2.65배 vs 3.50배).

**함정**
6. 🔴 **두 오차가 상쇄하면 "그럴듯한 틀린 값"이 된다 — 한쪽만 고치면 악화된다.** #22(값을 깎음)와 #24(3배 부풀림)가 서로를 가리고 있었다. **오늘 최대의 교훈이고, 배포 사고가 될 뻔했다.**
7. **자동 검증 "통과" 옆의 숫자를 읽어라.** #24는 E2E가 ✅를 찍은 화면에서 "75초에 240초"를 보고 잡았다. 통과 판정은 내가 물어본 것만 답한다.
8. **파생 가능한 값을 하드코딩하면 데이터와 어긋나는 건 시간문제다**(#9·#11). 라벨은 실제 기간에서 파생시킬 것.
9. **공용 헬퍼가 있는데 일부만 쓰면, 그 일부는 조용히 다른 답을 낸다**(#15 — `visitorKey`를 도넛만 안 썼다). 새 집계 전에 기존 식별 규칙을 먼저 찾을 것.
10. **아직 안 틀린 것도 고칠 수 있다**(#16 — 90일 컷은 미래에 물린다). 값 대신 **경계 도달을 실행 시점에 감지**하게 만들면 라벨이 스스로 정직해진다.

**설계**
11. **같은 규칙을 호출부마다 흩뿌리면 새 코드에서 한쪽을 빠뜨린다.** 추적 게이트는 **`_shouldSkipAnalytics`(사용자 행동) / `_shouldSkipSessionTracking`(세션·방문) 둘뿐**이고, 가르는 기준은 **"iframe 안에서 일어나도 진짜인가?"** 하나다. #24 자체가 이 누락에서 나왔다.
12. **"프레임 = 방문"이라는 암묵 전제가 세 테이블을 동시에 오염시켰다**(체류시간·`page_views`·`record_start`). 전제는 코드 한 줄이 아니라 **여러 지표에 동시에 새는** 방식으로 드러난다.
13. 🚨 **도형도 주장이다 — 데이터가 보장하는 관계만 그린다**(#26). 화살표·트리는 **순서와 포함관계를 주장**하는데, 이 화면의 계산은 무순서 교집합이라 **아무것도 보장하지 않았다**(포함관계가 5쌍 중 4쌍에서 깨짐). **바꾸기 전에 「이 도형이 주장하는 것」을 한 문장으로 적고 세어볼 것.** 곁들여: **비율의 분모는 툴팁이 아니라 화면 본문에** 적는다 — 툴팁은 마우스를 올려야 보이므로 "표기했다"의 근거가 못 된다(원칙 ①).
14. **이벤트는 코드 실행 시점이 아니라 사용자 행동에 묶여야 한다**(#25 — `record_start`가 허브 init에 묶여 프리로드마다 발사됐다).

### ⏳ 열림

> 📌 **발견을 등록할 땐 「⟹ 바뀌는 화면 숫자」를 반드시 적는다.** 안 적으면 **읽는 사람이 옆 항목과 합쳐 버린다** — 2026-07-20에 #25가 "분모도 부풀어 있었다"라고만 적고 어느 화면인지 안 밝힌 채 퍼널 항목(#18) 옆에 있어서, 다음 세션이 **"퍼널의 분모"로 읽고 완료 조건을 틀리게 잡았다.** 원리 2("그 숫자를 누가 읽는가")를 **진단할 때뿐 아니라 등록할 때도** 적용하는 것이다.

> **읽는 법**: 증상은 근원 원인 아래로 접었다. **`R3` 하나가 열린 항목의 절반을 설명한다** — 개별로 고치면 또 갈린다.

| # | 등급 | 내용 |
|---|---|---|
| 14 | 🟡 | `page_sessions.page`가 슬러그가 아니라 **표시 라벨**을 저장 → 이름을 바꾸면 버킷이 쪼개진다(`가격 & 규칙` 174행 vs `가격·이용안내` 65행, 옛 경로 잔여 약 224행=2%). **⟹ 영향**: **페이지 탭 가로막대**(같은 페이지가 두 줄로 갈림) + 회원 카드 페이지 분포. **⟹ 경로**: `supabase-client.js`의 `page_sessions` INSERT에서 저장값을 슬러그로 + **과거 행 소급 매핑**(불가분 감안) / 확인은 라벨 중복 버킷이 사라지는지 |

### 🔬 방문/이용 집계 정확도 (별도 계열)

- ① **`duration_sec=0` — ⚠️ 2026-07-19 실측으로 원인·범위 정정.** 기존 기재는 "14.7%, heartbeat 전 이탈"이었으나 **둘 다 틀렸다.** 전수 실측: 전체 11,710행 중 507행(4.3%)이 0이고 **그중 100%가 비회원**이다(회원 10,504행 중 **0건**). 원인은 이탈이 아니라 `_startAnonHeartbeat`이 INSERT 시 **`duration_sec: 0`을 하드코딩하고 이후 갱신하지 않는 것**([supabase-client.js:1122](../assets/js/supabase-client.js#L1122)) — **설계값이지 결손이 아니다.** 비로그인 체류는 `anon_sessions.today_seconds`에만 쌓이고 `page_sessions`로 역류하지 않는다.
  - 🚨 **따라서 "회원 쪽 `page_sessions`가 `duration_sec=0`으로 편향돼 있다"는 추론은 성립하지 않는다** — #21 판정 때 이 낡은 기재를 근거로 잘못 쓸 뻔했다.
  - ✅ **미해결 하위 질문 해소 (2026-07-20)**: "비회원 행의 절반이 `duration_sec > 0`인 이유" = **`page_sessions` INSERT 경로가 둘**이다. `_startAnonHeartbeat`(위, 0 하드코딩, `page`에 **슬러그**)와 **`script-nav.js`의 PAGE SESSION TRACKER**([script-nav.js:856](../assets/js/script-nav.js#L856), 실제 초, `page`에 **한글 라벨**, **3초 미만은 안 보냄**). 같은 세션의 행을 보면 `메인 4초`·`모임 플래너 3초`(라벨=트래커) 옆에 `index 0`·`club-schedule 0`(슬러그=heartbeat)이 나란히 있다.
    - 📌 **이게 #14(`page`가 라벨 저장)의 정체이기도 하다** — 라벨/슬러그가 섞인 게 아니라 **경로마다 다른 걸 넣고 있는 것**이다. #14에 착수하면 두 경로를 함께 봐야 한다.
    - 📌 **따라서 "비로그인 체류는 `page_sessions`로 역류하지 않는다"는 위 문장은 과장이다** — 트래커 경로로는 들어온다. 다만 **3초 미만·flush 실패분이 빠지고**, heartbeat 쪽은 **60초 단위**라 **둘 다 하한**이다(#3 판정의 근거).
  - 표시 영향은 그대로: 조건이 `todayS > 0`이라 **0이면 항목이 DOM에서 빠진다**(0으로 표시되는 게 아님).
- ② **과거 `session_key` NULL 70%** — 007 이전 legacy, **소급 불가**. 대상 아님.
- ③ ~~**이용시간 기기 중복** — 다기기 동시 사용 시 합산~~ → 🚨 **2026-07-19 정체 규명: 다기기가 아니라 `index.html`의 다중 프레임이다(#24).** 한 사람이 한 탭만 열어도 3배가 된다(E2E 실측). 다기기 합산도 이론상 성립하지만 **주된 원인이 아니었다** — 이 기재를 근거로 "다기기 쓰는 사람만의 문제"라 넘기지 말 것.
- ④ **`_startAnonHeartbeat` 자정 넘김 버그** — 1분 인터벌이 `today_seconds`만 갱신하고 `today_date`는 세션 시작값 고정. `page_sessions` 파생값을 우선 쓰므로 그 행이 없을 때만 발현.
- ⑤ **가상 페이지 키↔라벨 정합성** — 새 `_trackPvOnce` 키 추가 시 `page-labels.js` 동시 갱신 필요(안 하면 slug 노출, 조용히 발생).
- ⑥ **로그인 시 잔여 누적시간이 `page_sessions`에 안 들어감** — INSERT 경로가 `visibilitychange` 하나뿐. `beforeunload`/`pagehide`는 localStorage만 갱신. 설계는 `git show 4838691^:docs/PLAN_page_sessions_on_login.md`로 복원 가능.

**성격**: 대부분 "추적 로직 설계"(heartbeat 타이밍·기기 단위 세션) 문제라 Red + Plan. 코드 리팩토링보다 **집계 모델 재설계**에 가깝다.

---

## 5. 실행 계획 — 부위별

원칙에 비춰 **부위별로 분리**했다. 각 묶음은 독립적으로 끝낼 수 있고, 위에서부터 값이 크다.

| 묶음 | 내용 | 근거 원리 | 등급 |
|---|---|---|---|
| ~~**P1. 도달률**~~ | ✅ **2026-07-19 완료** — 아래 「이미 끝난 것」 참조 | ② | — |
| ~~**P2. 기준 표기 정리**~~ | ✅ **2026-07-19 완료** — #9·#11·#8·#16. 아래 「이미 끝난 것」 참조 | ① | — |
| ~~**P3. R3 해소**~~ | ✅ **2026-07-20 완료** — 질문별 단일 출처(기간=page_sessions / 누적=profiles), 각 자리 출처 표기. #12·#20·#21·#23 동시 종결 | ①② | — |
| **P3b. 드릴다운** | 🎯 **다음 작업.** 이벤트/페이지 → **그걸 한 사람들**로. 선행이던 P3가 끝나 이제 내려가도 숫자가 안 갈린다 | ⑤ | Plan 필요 |
| **P4. C단계(보드 연동)** | 남의 보드에 관리자 전용 섹션. 집계를 공용 모듈로 추출 + **오너 가드 필수** | ⑤ | Red+Plan |
| ~~**P5. 청소**~~ | ✅ **2026-07-19 대부분 완료** — #4·#17·죽은 렌더러 3개 삭제(순 −104줄), #7 축 눈금 60초 배수 고정. **#12(차트 소속)만 남았고 그건 청소가 아니라 설계**라 P3로 이관 | ③④ | 잔여=P3 |
| **P6. 집계 정확도** | 위 ①③④⑥ — 추적 로직 재설계 | — | Red+Plan |
| **P7. 저장 모델** | #14(page를 슬러그로) — 소급 불가분 감안 | ① | Red+Plan |

### 이미 끝난 것 (2026-07-19)
- **A단계**: 이벤트에 명·평균 + 전환율 사람 기준화(#10 동반 해결)
- **B단계**: 방문자 카드에 페이지 분포 펼침
- **P1 도달률**: 이벤트 탭 퍼널 위에 `📊 도달률 방문 67명 중 43명 64% · 회원 11/11(100%) · 비회원 32/56(57%)`.
  - **분모 선택이 이 작업의 본체였다** — 후보 두 개 중 `__visitor__`는 포함률 64%로 **탈락**, `page_sessions`만 100%(§3 함정 참조). 원칙 ②의 "분자가 분모에 포함되는지 실측"을 그대로 적용한 사례.
  - 검증: 4개 기간 전부 100% 초과 0건·분자≤분모·**분모 밖 0명**, `shot-admin-tabs.js` 회귀 17건 통과, 콘솔 에러 0. '전체' 값이 node 직접 집계와 **정확히 일치**(21%/70%/18%) — 서로 다른 두 경로의 교차 검증.
  - 부산물로 **#15·#16 발견** → 둘 다 같은 날 종결. #15는 "같은 화면 두 숫자" 문제라 **표기로 공존**시키고 분리 처리했다(사용자 판단: "묶어서가 아니라 분리해서").
- **#15 방문자 사람 세기**: 도넛·요약카드를 `visitorKey` 기준으로 통일. 단위 표기도 정정(총계·봇·평균 = `회`).
- **P2 기준 표기 정리**: #11(버튼=단위 선택기) · #9(유입 라벨을 실제 기간에서 파생 + 체류시간 제거) · #8(축 제목 + `⏱` + 막대 밖 표기) · #16(90일 경계 자동 감지).
  - 📌 **P1·#15가 "값"을 맞췄고 P2가 "이름"을 맞췄다.** 원칙 ①의 두 갈래(무엇을 셌나 / 그걸 뭐라 부르나)가 순서대로 닫힌 셈.

### 조건부 트리거 (지금 착수 금지)
- **집계 RPC(Ⓒ)**: `page_sessions` **5만 행 초과** 또는 관리자 페이지 로딩 **3초 초과** 시. 현재 11,500행 = 약 1년 여유.
  - 🔑 **진짜 난점은 집계 SQL이 아니라 클라이언트 인터랙션 의존**이다. 이 페이지는 `rows`를 메모리에 올려두고 날짜 네비·필터를 즉시 재계산한다(`dedupUserPageDay`·`buildPageMap`·`buildUserMap`·`buildAnonUserMap`·`filterByRange`). RPC로 가면 **UI 재작성에 가깝다.**
  - **선행**: 「2단계(어떤 집계를 추가할지)」 확정 후. 먼저 만들면 갈아엎게 된다.
- **탭 구조 압축안**(2026-07-02 기획, 미결정): `요약/방문자/유입·페이지/행동` 재구성. 선행 조건이던 카운팅 기준 통일은 완료 → **적용 여부만 결정하면 됨**. P2·P3와 범위가 겹치므로 통합 판단할 것.

---

## 5-1. 이 화면을 만질 때 깨뜨리면 안 되는 제약

**변경 이력이 아니라 제약만 남긴다** — "무엇이 바뀌었나"는 git log가 갖고 있고, 여기 있어야 하는 건 **모르면 다시 깨뜨릴 것**뿐이다.

| 제약 | 왜 |
|---|---|
| 🚨 **새 `trackEvent` 타입은 `EVENT_FAMILIES`에만 등록하면 된다** | 조회 목록이 여기서 파생된다. 예전엔 두 벌이라 어긋나 **1,493행 중 77%가 조회조차 안 됐다**(#13) |
| 🚨 **회원 카드 페이지 분포는 `dedupUserPageDay`를 일부러 안 거친다** | dedup하면 체류가 그날 첫 세션만 합산돼 과소집계된다. 비회원 경로도 원본을 쓰므로 이쪽이 일관적이다. **기간 선택(R3)을 붙일 때 이 결정을 깨지 말 것** |
| 🚨 **"방문자 N명"은 소스가 둘이고 호환되지 않는다** | 도넛(`__visitor__`)과 도달률(`page_sessions`)이 다른 건 버그가 아니라 모집단 차이다. 비율의 분모로는 `page_sessions`만 쓴다 — 상세는 §3 |
| ⚠️ **`renderFunnel`은 `reach` 없이 불릴 수 있다** | `loadEventStats`가 `renderCharts`보다 늦게 끝나 최초 1회는 도달률 인자가 없다. 없으면 그 줄을 생략하는 구조를 유지할 것 |
| ⚠️ **legacy 행(`session_key` 없음, 2026-07 이전)은 `visitorKey`가 행마다 다른 키를 준다** | 그 구간의 "명"은 **상한값**이다. 소급 보정 불가 |
| ⚠️ **카드 펼침 토글은 이벤트 위임이어야 한다** | 카드가 매 렌더 새로 생기고, 같은 카드 안 밴 버튼과 오발동하면 안 된다 |
| 🚨 **추적 게이트는 둘뿐이다** | `_shouldSkipAnalytics`(사용자 행동) / `_shouldSkipSessionTracking`(세션·방문). 가르는 기준은 **"iframe 안에서 일어나도 진짜인가?"**. 흩뿌리면 새 추적에서 한쪽을 빠뜨린다(#24가 그 사례) |

---

## 6. 재조사 금지 — 기각된 가설

「관리자 금일이용데이터 간헐 미표시」 추적에서 **근거를 갖고 기각**된 것들. 다시 파지 말 것.

- ⓐ **로드순서 레이스** → 스크립트가 전부 classic이고 인라인 IIFE가 `supabase-client.js` 뒤라 `window.CottageDB`는 항상 정의됨.
- ⓑ **`tried` 래치 + 1200ms 게이트** → `isOwner()`가 동기 localStorage 읽기라 그 시점에 확정됨.
- ⓒ **KST 타임존 불일치** → 읽기/쓰기가 전부 동일 공식(`Date.now()+9h`).
- ⓓ **"조회 실패가 조용히 빈 값"** → 감지기 1~4단계가 켜진 상태에서 콘솔 대괄호 라벨 에러 **0건**.

**단일버그 추적은 2026-07-18에 종료되고 페이지 전체 감사로 대체됐다.** 남은 유력 후보는 ③ 에러 수집 부재(=`console.error`가 우리에게 안 옴)와 위 ①`duration_sec=0`.

---

## 관련 문서

- [PROJECT_STATE.md](PROJECT_STATE.md) — 진행 상태·시작점 (이 문서가 관리자 분석의 상세를 가짐)
- [db-schema.md](db-schema.md) — 테이블·RLS·`max-rows`
- [js-api.md](js-api.md) — `getEventCounts`·`getPageAnalytics` 등 계약
- [PLAN_funnel_analytics.md](PLAN_funnel_analytics.md) — 퍼널 원기획(2026-06-27). 2단계 "unique 카운트 기반 전환율"은 **A단계로 부분 실현**됨
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) §6 — 이용시간/방문자 추적 흐름
