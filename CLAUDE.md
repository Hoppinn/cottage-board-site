# CLAUDE.md

Claude Code가 이 프로젝트에서 따라야 할 규칙.

> **이 파일의 편집 기준 (2026-07-20 감사에서 확립)**: 규칙을 추가하려면 **"이게 없었으면 실제로 틀렸을 사건"**을 함께 적는다. 못 적으면 넣지 않는다.
> 남길 가치가 있는 건 두 종류뿐이다 — ①**추론으로 도달 못 하는 사실** ②**내가 무엇을 검증할지 바꾸는 규칙**. **나를 멈추게 하거나 승인받게 만드는 규칙은 기본적으로 넣지 않는다.**
> 잘라낸 규칙과 그 이유는 [docs/_archive_claude_rules.md](docs/_archive_claude_rules.md), 원문은 `git show backup/claude-md-full-2026-07-20:CLAUDE.md`.

---

## 세션 시작 시 필독

- `docs/PROJECT_STRUCTURE.md` — 페이지 구조, JS 역할, 데이터 흐름
- `docs/PROJECT_STATE.md` — **열린 것만**: 현재 목표·종료 조건·버그·추후 작업·다음 시작점 (완료 내역은 여기 없다 → git log)

### 🚩 읽고 나서 「세션 계약」 5줄을 **내가 만들어 제시한다**

**사용자가 써주는 게 아니다.** 사용자 입력은 "관리자 페이지 계속하자" 정도면 충분하고, **문서에서 뽑아내는 건 내 일**이다. 명백하면 **확인을 요구하지 말고 그대로 진단을 시작**한다 — 문서끼리 충돌하거나 되돌릴 수 없는 실행이 걸릴 때만 묻는다.

| 줄 | 어디서 뽑나 |
|---|---|
| **목표** | `PROJECT_STATE` 시작점 표의 🚩 |
| **종료 조건** | 같은 자리의 🏁 — 없으면 **내가 정해서 제시**한다 |
| **이번에 닫을 것** | 시작점 표 1번 + 그 항목의 「⟹ 영향·경로」 |
| **제외 범위** | **판단 범위를 화면으로 긋되 파일로 긋지 않는다** — 그 화면의 데이터를 고치는 데 필요한 **발생 지점 코드는 건드린다**(예: 관리자 통계를 고치려면 홈의 이벤트 발사 코드를 만져야 한다). 무관한 UI·기능 개선으로 번지지 않는 게 경계다 |
| **검증 기준** | 그 항목의 「⟹ 경로」에 적힌 완료 확인법 |

⚠️ **묶음은 처음부터 가정하지 않는다.** 각 항목을 **독립으로 진단**하고, **공통 원인·코드 경로·검증 환경이 실제로 확인된 뒤에만** 묶는다(A-3).

> **사건(2026-07-20)**: 이 절차를 "사용자가 매 세션 붙여넣을 문구"로 만들어 제시했다가 반려됐다. **자동화해야 할 판단을 사용자 체크리스트로 바꾼 것**이고, 「기본 자세는 제안이지 질문이 아니다」(D-11)를 정면으로 어긴 것이다.

작업 주제가 바뀌면 아래 표의 파일을 읽는다. **읽기 전에 수정하지 않는다.**

| 작업 종류 | 필독 |
|---|---|
| DB 테이블/컬럼 | `docs/db-schema.md` (영구 식별자 규칙 포함) |
| CottageDB 함수 / window 전역 | `docs/js-api.md` |
| localStorage / 세션 키 | `docs/ls-schema.md` |
| 인증·프로필·사진 | `PROJECT_STRUCTURE.md` §3~5 |
| 이용시간·체류 추적 | `PROJECT_STRUCTURE.md` §6 |
| 모달·iframe·시트 | `PROJECT_STRUCTURE.md` §2-A |
| 게임 데이터·파이프라인 | `PROJECT_STRUCTURE.md` §7~8 (작업 전 `npm run check`) |
| UI/CSS·sticky·깜빡임 | `docs/DESIGN_RULES.md` |
| 업적/칭호/캐릭터/교환권 | `docs/achievement-system.md` (SSOT) |
| 관리자 분석·방문/이용 집계·퍼널 | `docs/admin-analytics.md` (SSOT) |

**변경 시 같은 커밋에 해당 문서도 갱신한다.** "나중에 한꺼번에"는 compact 때 날아간다.

---

## 저장 위치 — 한 사실은 한 곳에만 (single source of truth)

**같은 사건을 두 곳에 설명하지 않는다.** 다른 문서에서 필요하면 **복사하지 말고 가리킨다.**

| 정보 | 유일한 주소 |
|---|---|
| 현재 목표·종료 조건·열린 작업·미확정 판단·다음 시작점 | `docs/PROJECT_STATE.md` |
| 완료한 변경과 그 검증 | **git commit** |
| 앞으로도 유효한 시스템 구조·위험 | 해당 도메인 문서 |
| 여러 세션에 지속되는 협업 원칙 | **이 파일** (메모리와 나누지 않는다) |
| 세션 과정·틀린 가설·반성 | 임시 scratchpad — **종료 시 폐기** |

### 세션 종료 = 요약 작성이 아니라 상태 압축

⏱ **언제**: **"세션 종료"를 보고하기 전에** 아래를 자동 수행하고, 필요한 정리와 재검사까지 끝낸 뒤에만 종료를 선언한다. 단 사용자가 종료 절차를 **가정형으로 묻기만 한 경우엔 실행하지 말고 절차만 설명한다**(질문은 실행 명령이 아니다 — D-11).

1. **닫힌 항목은 PROJECT_STATE에서 지운다.** 완료 목록·커밋 목록·세션 회고를 거기 넣지 않는다.
2. 잃으면 안 되는 **기술적 판단은 도메인 문서 한 곳**에만.
3. **목표·종료 조건·다음 원자 작업**만 갱신한다.
4. 사용자에게는 완료 요약을 **말하되 문서에 복사하지 않는다.**

🔍 **검사 문장**: 여러 건을 닫은 세션이면 **PROJECT_STATE의 줄 수는 줄어야 한다.** 늘었다면 새로 열린 작업이나 필수 판단이 무엇인지 대고, 못 대면 압축한다.

> **사건(2026-07-20)**: "누락없이 정리해"를 **"빠짐없이 §0에 적기"**로 읽고 닫은 5건을 22줄로 늘어놨다. **거의 전부가 도메인 문서·git log에 이미 있었다.** 같은 사실이 두 곳에 있으면 다음 세션엔 "어느 쪽이 최신이지"가 된다. **누락 없음 = 제자리에 한 번 남기고 나머지를 지우는 것**이지 전부 복사가 아니다.

### 규칙·메모리로 승격하기 전 문턱

다섯 중 **하나라도 걸리면 새로 만들지 않는다** — ①여러 세션에 걸쳐 안 변하나 ②기존 원칙에 이미 포함되지 않았나 ③사건 설명 없이 한두 문장으로 쓸 수 있나 ④프로젝트 상태·기술 사실을 원칙으로 잘못 올리는 건 아닌가 ⑤git·도메인 문서에 이미 있는 걸 반복하는 건 아닌가.

🚫 **피드백 한 건마다 새 메모리 파일을 만들지 않는다** — 같은 범주의 기존 원칙에 **한 줄로 통합**한다.
> **사건(2026-07-20)**: 지적 하나를 받고 13줄짜리 메모리 파일 신설 + 인덱스 + PROJECT_STATE + 커밋 메시지에 **같은 서사를 네 번** 적었다. 파일은 지우고 이 파일 D-11 한 곳으로 합쳤다.

### 옆 문제를 발견했을 때 — 묻지 말고 셋 중 하나로 판정

- **같은 원인·코드 경로 + 작고 안전** → 그 자리에서 고치고 검증한다.
- **가치 있지만 현재 목표와 별개** → PROJECT_STATE에 **항목 하나만** 등록하고 하던 일을 계속한다.
- **크거나 되돌리기 어려움** → 최선안을 제안하고 **실행 전에만** 승인받는다.

🚨 **"사용자 영향이 작다"는 우선순위를 낮출 근거지 영구 방치의 근거가 아니다.** 반대로 **불필요하다는 이유만으로 현재 세션을 무한 확장하지도 않는다** — 위 셋 중 어디인지 먼저 판정한다.

---

## 닫기 우선

**등록은 공짜고 닫는 건 판단이 필요하다.** 그래서 방치하면 **결정할 자리마다 문서를 쓰는** 행동이 나온다 — 일하는 것처럼 보이지만 아무것도 확정하지 않는다.

> **사건(2026-07-19)**: 관리자 페이지를 정리하려던 세션에서 발견 6건을 등록하고 커밋 24개를 쌓았는데 **화면은 그대로**였다. 근원 원인은 고쳤지만 **그것이 설명하는 증상 4건을 닫지 않고 나란히 등록**했다.

### A. 착수할 때 — 세 가지를 먼저 적는다

1. **Definition of Done: "이번에 무엇이 닫히는가"를 한 문장으로.** 끝날 때 그 문장이 참이 됐는지 대조한다. 참이 아니면 완료가 아니다. **이 파일에서 유일하게 검사 가능한 규칙**이라 나머지가 다 무너져도 이건 지킨다.
   - 명확한 구현 작업 착수 시 `.claude/current-task.json`에 목표와 완료 조건을 기록한다.
2. **완료 조건은 "사용자가 보는 것이 달라진다"로.** "항목 N건 처리"로 쓰면 모든 갈래가 똑같이 정당해 보여 목표에서 멀어져도 모른다.
3. **묶어서 할 거면 근거를 적는다 — 같은 원인·같은 코드 경로·같은 검증 환경 중 하나를 공유할 때만 묶는다.** 주제나 화면이 비슷한 건 근거가 아니다.
   > **사건(2026-07-20)**: `record_start` 오발사와 `home_record_write_click` 0건을 "둘 다 기록 계열"이라며 묶었는데, 실제로는 **다른 파일·다른 원인 유형·다른 검증 방법**이었다.

### B. 순서 — 목표 안에서 쉬운 것부터

4. **다른 작업으로 넘어가기 전 현재 목표의 종료 조건을 확인한다.** 못 채웠으면 **미완 이유와 재개 조건**을 적는다. 다음 게 더 쉽거나 묶여 있다는 이유만으로 건너뛰지 않는다.
5. **열린 목록이 많아 보이면 먼저 셋으로 분류한다** — ①확인만 하면 닫히는 것 ②판단이 필요한 버그 ③기획·디자인. **①부터 닫고 ③은 목록에서 분리**한다. 숫자가 줄어야 남은 게 보인다.
   > **사건**: 열린 34건 중 **12건이 ①**이었다(이미 고쳐졌거나 원인이 밝혀진 것). 죽은 항목에 가려 진짜 작업 10건이 안 보였다.
6. **순서는 "쉬운 것 → 근원이 같은 묶음 → 무거운 것".** 단 **순서보다 「지금 사용자의 목표」가 먼저**다 — 쉬운 것·묶인 것은 **목표 안에서** 고르는 기준이지 목표를 바꾸는 근거가 아니다.
   > **사건**: 목표(관리자 페이지)가 절반 남았는데 "쉬운 것부터"만 보고 **다른 화면**을 다음 작업으로 제안했다. 충돌처럼 보였지만 **목표 화면 안에도 쉬운 게 5건 있었다** — 안 쪼갠 게 문제였다.
7. **근원 원인을 찾으면 그것이 설명하는 증상을 같은 작업에서 닫고, 그 자리를 정리(리팩토링)까지 한다.** 못 닫으면 이유 한 줄. 고쳐만 두고 구조를 남기면 같은 병이 다시 난다.
   - **Vertical slice** — 사용자가 보는 변화 하나를 검증·커밋까지 닫고 다음으로.
8. **새 갈래가 흥미롭다는 건 따라갈 이유가 아니다.** "사실이고 흥미롭다"는 "끝났다"가 아니다. 등록만 하고 지나간다.

### C. 기록할 때

9. **문서는 닫을 때 한 번 쓴다.** 발견마다 왕복하지 않는다 — 그 왕복이 위 사고에서 커밋의 절반이었다.
10. 🚨 **발견을 등록할 땐 증상만 적지 않는다. 두 가지를 함께 적는다.**
    - **⟹ 영향 대상** — 이게 바꾸는 **실제 출력·화면·데이터·동작**과 **그걸 읽는 소비자**. (관리자 분석 문서에선 「바뀌는 화면 숫자」로 구체화)
    - **⟹ 수정·검증 경로** — **어디를 고치고, 무엇으로 완료를 확인하는가.**
    - **둘이 하는 일이 다르다**: 영향 대상은 **오독을 막고**, 수정·검증 경로는 **묶을지 말지를 판정**한다(A-3). 영향이 달라도 경로가 같으면 묶을 수 있고, 영향이 같아도 경로가 다르면 따로 한다.
    > **사건**: `record_start` 항목에 "분모도 부풀어 있었다"라고만 적고 **어느 화면인지 안 밝힌 채** 퍼널 항목 옆에 뒀더니, 다음 세션이 "퍼널의 분모"로 읽고 완료 조건을 틀리게 잡았다.
    > ⚠️ **원인은 문서만이 아니었다.** 같은 문서에 **"퍼널은 `record_*`를 안 쓴다"**고 **명시돼 있었다.** 다음 세션은 **서로 충돌하는 두 정보를 읽고, 충돌을 해소하지 않은 채** 자연스러운 쪽으로 연결했다. → **문서의 두 설명이 충돌하면 그럴듯한 쪽으로 잇지 말고 코드·데이터로 해소한다.**


### D. 사용자가 되물었을 때 — 재검증이지 번복이 아니다

11. **기본 자세는 "제안"이지 "질문"이 아니다.** 사용자는 **웬만하면 내 판단을 따르고 이상할 때만 개입**한다. **"A가 맞아?"의 답은 "A가 맞다, 근거는 X" 또는 "A가 아니라 B다, 근거는 Y"**다 — 되물어서 결정을 돌려주지 않는다.
    - 🔍 **검사 가능한 형태(2026-07-20 추가)**: 보고를 내보내기 전에 **물음표로 끝나는 문장**을 전부 찾아, 각각 **"이 답을 내가 이미 갖고 있나?"**를 묻는다. 갖고 있으면 **제안으로 바꾼다.** 남겨도 되는 건 **내가 가질 수 없는 정보**뿐이다 — 사용자의 일정·취향·사업 판단, 그리고 되돌리기 어려운 실행의 승인(D-14).
    > **사건(2026-07-20)**: D-11이 이미 있는데도 한 세션에서 **세 번** 어겼다("계속할까요" / "지금 착수할까요" / "등록만 해둘까요"). 마지막 건은 판단까지 끝내놓고 물은 것이라 더 나빴다 — 옆 문제 처리는 「저장 위치」절 끝의 **셋 중 하나로 판정**한다.
12. **입장을 바꾸려면 「무엇이 바뀌었는지」를 한 줄로 댄다.** 셋 중 하나여야 한다 — ①**새 증거·사실** ②**새로 확인된 목표·제약** ③**기존 정보에서 발견된 누락·모순·오류**. 셋 다 없이 **어조가 강해졌다는 이유로** 바뀌면 그건 **순응**이고, 그땐 기존 판단을 유지하고 근거를 다시 설명한다.
    > ⚠️ **"새로 알았다"를 만들어내지 말 것**: "P3b가 Plan 필요라는 걸 새로 알았다"고 적었는데 **문서에 이미 있었고 나도 읽은 상태**였다(=③). ③이면 ③이라고 적어야 구멍이 안 생긴다.
13. 🚨 **의문이 제기된 순간에는 코드·문서·커밋을 멈춘다.** 재검증해 **결론을 확정한 뒤 확정된 것만** 반영한다.
    > **사건**: `판단 → 반문 → 즉시 번복 → 문서 수정 → 재반문 → 재번복 → 다시 수정`이 실제로 일어나 **내 흔들림이 프로젝트 문서에 상태로 퇴적**됐다.
14. 🚫 **"판단하지 않는 영역"은 없다. 있는 건 "실행하지 않는 영역"이다.** 디자인·문구·공개 범위도 **어느 쪽이 목적에 맞는지는 판단해서 추천**한다. 다만 **되돌리기 어렵거나**(DB·파일 삭제·배포) **사용자의 취향·가치·사업 책임이 걸린** 결정은 **추천과 실행을 분리**해 실행 전 승인을 받는다.
15. 🚨 **규칙을 방패로 쓰지 않는다.** 「분리해서」·「Plan 후 승인」은 무모함을 막는 장치지 **끝내지 않을 핑계가 아니다.**

### E. 완료 보고는 중학생이 읽어도 이해되게

함수명·발견번호 없이 **무엇이 어떻게 달라졌는지** 전달돼야 한다.

- ❌ "`_isEmbeddedFrame()` 가드 추가로 #24 종결, E2E 3.50→0.88배"
- ✅ "홈 화면이 안 보이는 창 3개를 띄워두는데, 그 3개가 각자 '이 사람 접속 중'이라고 따로 보고하고 있었습니다. 1시간 있으면 3시간으로 기록됐어요. 이제 대표 하나만 보고합니다."

**숫자엔 좋은지 나쁜지를 붙인다.** 🚨 **"정상입니다"는 재고 나서 말한다** — "0.88은 정상"이라고 근거 없이 단정했다가 "그럼 12%는 사라지는 거냐"는 지적을 받았고, **재보기 전까지 나도 몰랐다.**

⚠️ **이걸 적는 것만으로는 안 고쳐진다.** 위 규칙 상당수가 이미 다른 이름으로 있었는데도 2026-07-19에 전부 어겼다(있는 규칙을 오히려 방패로 썼다). **유일하게 검사 가능한 형태는 A-1의 한 문장이다.**

## 검증

**"될 것 같다"로 커밋하지 않는다.** 로직 추적 / node 테스트 / 엣지케이스(0·null·날짜 경계·SELECT 실패) 중 하나 이상으로 확인 후 커밋.

### 검사기를 먼저 의심한다

**"0건"·"전부 통과"는 도구가 고장 난 것과 구별되지 않는다.** 알려진 실패를 일부러 심어 잡히는지 보기(음성 대조군) 전엔 통과를 신뢰하지 않는다.

> **사건**: 2026-07-16 하루에 5번, 2026-07-20 하루에 3번 검사기가 조용히 거짓말했다.

- **bash heredoc·`node -e`는 정규식 백슬래시와 백틱을 먹는다** — `'\\b'`가 백스페이스 문자가 돼 아무것도 매치 못 함. 검사 스크립트는 **Write 툴로 파일 생성**할 것.
- **DB/HTTP 조회는 상태 코드를 본다** — 컬럼명 오타로 온 HTTP 400을 "0행"으로 오독한 적 있음(실제 10,975행).
- **컬럼·필드명은 추측하지 말고 마이그레이션 SQL에서 확인한다.**
- **문자열·주석 안의 이름을 코드 참조로 세지 않는다.**
- **손으로 만든 스캐너로 JS를 파싱하지 않는다** — IIFE를 함수명으로 세고 정규식 리터럴을 문자열로 읽어 "141개 중 138개 불일치"라는 그럴듯한 거짓을 냈다. 답은 대개 질문을 좁힌 grep 한 줄이다.
- **Grep(ripgrep)의 `\b`는 유니코드 인식이라 한글 조사에서 안 끊긴다** — `\bfoo\b`가 `foo의`를 못 찾는다. 문서·주석 언급을 셀 땐 `\b` 없이. (코드 식별자 뒤엔 조사가 못 붙으므로 죽은 코드 판정엔 무영향.)
- **이 환경의 Grep은 `content` 출력에서 `/`를 `\`로 표시한다** — `a/b`가 `a\b`로 보여 문법 오류로 오인하기 쉽다. 슬래시가 걸린 판단은 Read로 재확인.
- **브라우저 자동화 산출물을 리포 안에 쓰지 않는다** — Live Server가 감시 중이라 쓰는 순간 리로드가 걸려 페이지가 초기화된다. 증상이 "탭이 안 열림"으로 나타나 스크립트를 고치게 되지만 원인은 **출력 경로**다. → OS 임시폴더.

**모순을 만나면 그게 단서다.** 검사 결과가 알려진 사실과 충돌하면 **사실을 의심하기 전에 검사기를 의심할 것.**

### 자동 검증 통과는 육안 확인을 대체하지 않는다

> **사건**: assert가 전부 PASS인 뒤 스크린샷에서 잡은 것 — 짧은 막대에서 흰 글씨가 막대 밖으로 넘침(#8), 카드 38분 vs 표 61분(#21), E2E ✅ 옆의 "75초에 240초"(#24). **마지막 건 안 봤으면 3.5배 부푼 값을 배포했다.**

**assert는 내가 물어본 것만 답한다.** UI를 건드린 커밋은 스크린샷을 **읽을 것**(찍는 것과 다르다).

### 런타임 값 검증

레이아웃·스크롤·sticky·position·offset·CSS 변수 버그는 **추론으로 고치지 않는다.** `getComputedStyle()`·`getBoundingClientRect()`·`offsetHeight`로 실제 값을 먼저 본다.

- `calc()`/`var()`/`env()` 포함 값에 `parseFloat()` 금지(NaN). 실제 DOM 크기를 재거나 CSS가 처리하게 둔다.
- **"수정 완료"는 실제 재현 확인과 함께만.**

### 반복 패치 정지

같은 파일·함수·선택자를 **2회 수정해도 안 되면 멈추고 보고**한다. 3회차부터는 원인 가설·검증 방법·롤백 방법을 먼저 적는다.

**스크립트에도 같이 적용** — 같은 스크립트를 2번 고쳐도 실패하면 스크립트가 아니라 **구현 로직·환경 전제**를 의심한다. (Playwright 출력 경로 사건이 정확히 이것이었다.)

### "없다"는 실측 후에 말한다 (evidence before conclusion)

**"기능·동작이 없다"를 grep 한 번으로 단정해 문서·커밋에 적지 않는다.**

> **사건**: 배경클릭 닫기가 실제로 있었는데 "없다"고 커밋에 적었다가 반박당함(2026-07-17). 2026-07-20에도 "퍼널이 이 이벤트를 쓴다"는 문서 전제가 틀린 걸 착수 후에 발견.

같은 항목을 언급한 문서가 여럿이면 **상태를 일치시킨다**(한쪽만 고치고 지나가기 쉽다).

---

## DB 함수 에러 처리

**고칠 게 있는 실패만 로그한다.** 기준은 "이게 울렸을 때 내가 고칠 게 있는가?"

- **있다** → `console.error('[실패 지점 이름]', e)` 후 반환. `catch (_) { return []; }`로 삼키지 않는다.
- **없다** → 삼키되 **왜 삼키는 게 맞는지 주석**을 남긴다.

감지기의 가치는 **"울리면 진짜"**에서 나온다. 조용한 실패는 화재를 가리고, 정상 경로마다 울리는 로그는 진짜 화재를 가린다.

⚠️ **`try/catch`만으론 부족하다.** **supabase-js는 쿼리 오류에 예외를 던지지 않고** `{ data: null, error }`를 반환한다. `const { data }`만 받으면 **컬럼 오타·RLS 차단·테이블 없음이 전부 조용히 빈 배열**이 된다. **`{ data, error }`로 받아 `if (error) console.error(...)`** 할 것.

🚨 **`error`를 받아도 부족하다 — 행 수 자체가 거짓말한다.** 아래 둘은 `error`가 `null`이고 `catch`도 안 걸린다.

- **행 수가 정확히 `1000` → 절단.** PostgREST `max-rows` 상한이고 클라이언트 `.limit()`으로 못 넘는다. **실제 사고**: 11,439행 중 91%가 조용히 사라져 "90일 분석"이 실제로는 7일이었고, 총 체류시간이 12시간으로 보였으나 실제 276시간.
- **행 수가 `0` → RLS 의심.** **이 프로젝트엔 `authenticated` 역할이 존재할 수 없다**(카카오 OAuth). 정책을 `to authenticated`로 걸어 1,452행이 몇 주간 안 보인 적 있음. **정책의 `to`는 항상 `anon`.**
- **판정법**: `count: 'exact'`로 실제 행 수와 대조하기 전엔 **"데이터 없음"으로 결론짓지 않는다.** 없는 테이블은 `count`가 `null`, 비었지만 존재하면 `0`.
- ⚠️ **`max-rows`는 대시보드 설정이라 마이그레이션에 안 남는다** — DB 재구축 시 1000으로 돌아가 조용히 재발한다.

**RLS**: 이 프로젝트 기본은 `DISABLE`. 테이블 생성 시 마이그레이션에 `ALTER TABLE {name} DISABLE ROW LEVEL SECURITY;`를 명시하고, 실행 직후 anon 키 SELECT로 확인한다.

**반환 계약은 유지한다** — 실패 시 `[]`/`null` fallback을 throw로 바꾸면 빈 배열을 전제로 렌더하는 호출부가 깨진다. 바꾸는 건 **관측(로그)이지 동작이 아니다**.

---

## 데이터·보안

- **방문자·통계 버그를 고칠 때 데이터 초기화 금지.** 원래 있어야 할 값으로 복원하는 방식으로만.
- 브라우저 검증 스크립트 작성 시 `/browser-verify` 필독 — 운영 DB에 실제 쓰기가 발생한다.

---

## 신규 생성 전 기존 검색 (reuse before create)

새 localStorage 키·DB 테이블·전역 함수·CSS 클래스를 만들기 전에 `ls-schema.md`·`db-schema.md`·`js-api.md`에서 **같은 용도가 이미 있는지** 찾는다. 확장 가능하면 그 방향으로, 신규가 불가피하면 이유를 커밋 메시지에 적는다.

> **사건**: 사람 세는 규칙(`visitorKey`)이 이미 있었는데 도넛과 요약카드만 자체 계산을 해서 **비회원 수가 완전히 틀렸다**(#15). 같은 개념에 공용 헬퍼가 있는데 일부만 쓰면 그 일부는 조용히 다른 답을 낸다.

---

## 외부 의견

GPT·다른 Claude 등 제3자 의견은 **구현 지시가 아니라 감사 입력**이다. 현재 코드·문서·DB 기준으로 재검증하고, 차이가 있으면 먼저 보고한다. 출처를 안 밝혀도 문체(구조화 목록, 프로젝트 문맥 없는 일반론)로 판단해 동일 적용.

> **사건**: 2026-07-20 다른 인스턴스가 "훈계를 지우라"고 제안했는데, 그 기준이면 **검사기 의심·육안 확인까지 지워진다.** 그걸 지웠으면 그날 작업이 틀린 채로 배포됐다. **파일만 읽은 판단과 그날 무슨 일이 있었는지 아는 판단은 다르다.**

🚨 **지적은 「문제 가설」이지 「해결안」이 아니다.** 문제가 실재함을 확인했더라도 **해결 모델은 목적·코드·데이터에서 다시 설계한다.** 지적이 암시하는 형태를 그대로 답으로 쓰면 **문제만 검증하고 해답은 검증 안 한 상태**가 된다. (반대로 "외부 지적이니 안 바꾼다"도 틀렸다 — 맞는 지적에 고집을 부리게 된다.)

- **판정법**: 표현·도형·지표를 바꿀 땐 **그것이 주장하는 관계를 한 문장으로 적고 데이터가 보장하는지 센다.** 순서·분기·포함이 증명되지 않으면 그 도형을 쓰지 않는다.
> **사건(2026-07-20)**: "퍼널이 선형으로 보인다"는 지적을 받고 **분기 트리**로 바꿨는데, **내가 방금 출력한 화면 숫자가 이미 포함관계를 부정**하고 있었다(5쌍 중 4쌍에서 "B를 한 사람이 A를 안 함"). **지적은 맞았고 내 해답이 틀렸다.** 같은 자리를 하루에 세 번 고쳤고 2·3차가 같은 실패였다.

---

## Plan이 필요한 작업

**DB 테이블/컬럼/RPC 신규 생성 또는 변경**, **파일 삭제**, **인증 구조 변경** — 되돌리기 어려운 것만. Plan → 승인 → 실행.

```
읽을 파일: [파일:줄범위]
변경할 대상: 기존 X → Y (재사용/신규, 신규면 이유)
영향 파일: A.js, B.html
보존해야 할 기존 동작: (있으면)
위험요소: 첫 번째로 실패할 가능성이 높은 지점
```

> **사건**: 마이그레이션 012 Plan이 "행이 없으면 아무것도 안 한다"를 미리 짚어둔 덕에, RPC를 upsert로 만들어 **빈 프로필 행을 양산할 뻔한 걸** 막았다.

그 외에는 **묻지 말고 판단해서 진행한다.**

---

## Git

- 작업이 완료될 때마다 커밋한다.
- 🚨 **`git push`는 이 환경에서 권한으로 차단돼 있다 — 내가 실행할 수 없다.** 그러니 **"push 해드릴까요"라고 묻지 말고, "push는 직접 해주세요"라고 안내**한다. 사용자가 "해봐라"고 해도 결과는 거부이고, 시도 자체가 왕복만 늘린다.
  > **사건**: 2026-07-20 같은 세션에서 세 번 반복했다. 사용자 표현 — "맨날 왜 같은 시행착오를 반복하는지 답답하다".
- 한 커밋에 feat와 refactor를 섞지 않는다.
- 이 저장소에는 Claude Code 세션을 동시에 하나만 돌린다.
- 여러 건을 한 지시로 받으면 **건별 승인 없이 연속 처리**하고, 항목마다 별도 커밋 후 마지막에 한 번 보고한다.
- `db-schema.md`·`ls-schema.md`·`DESIGN_RULES.md`·`PROJECT_STRUCTURE.md`가 바뀐 커밋이면 보고에 **"⚠️ 웹 프로젝트 지식 재업로드 필요: [파일명]"**을 포함한다.

---

## 이 파일 자체의 감사 (2026-07-20 시행 → 재감사 예정)

**시행 내역**: 331줄 → 이 버전. 규칙마다 "없었으면 틀렸을 사건"을 대게 해 11건을 잘라내고([아카이브](docs/_archive_claude_rules.md)), CSS·iframe 4건을 주제별 문서로 이관했다.

**검증 기간을 두고 다시 감사한다.** 다음 조건 중 **먼저 오는 것**이 트리거:
- 잘라낸 규칙 때문에 **사고가 났을 때** (즉시)
- **작업 세션 5회**를 지났을 때

**재감사 때 볼 것** — 아래 네 가지이며, **판단이 아니라 기록으로 답한다**:
1. 잘라낸 11건 중 **되살린 게 있나?** 있으면 어떤 사고 때문이었나
2. **조건부 유지 항목**(아래)이 실제로 발동했나
3. 남은 규칙 중 5회 세션 동안 **한 번도 안 걸린 것**이 있나 → 다음 자를 후보
4. 발동 기록이 있어도 **모든 세션에 상주해야 하는가**를 별도 판정한다 — 특정 작업에서만 발동하는 규칙은 도메인 문서·매핑표·커맨드 이동 후보(이번에 이동한 `browser-verify`가 **포인터 경유로 실제 발동했는지**를 기록으로 판정). 단 **매 세션 발동이 필요한 규칙은 커맨드로 이동할 수 없다 — `wrap` 이동 후 복원 사례로 확인(2026-07-21)**, 커밋마다 발동하는 검증 계열(검사기 의심·육안 확인·DB 에러 처리)도 상주 유지

🔬 **조건부 유지 — 미검증 상태 (2026-07-20 등록)**

「닫기 우선」과 「완료 보고는 중학생 수준으로」는 **2026-07-20에 신설돼 아직 검증되지 않았다.** 근거가 "이게 있어서 사고를 막았다"가 아니라 **"이게 없어서 사고가 났다"**뿐이다. 이 파일의 기준을 엄격히 적용하면 자격 미달이므로 조건부로 남긴다.

🚨 **발동하면 그 자리에서 한 줄 기록한다** — **그 작업의 커밋 메시지에** "「닫기 우선」 N번이 X를 막았다" 형태로(재감사 때 `git log --grep`으로 센다). **기록이 없으면 재감사 때 판단할 근거가 없어 또 한 번의 취향 투표가 된다.**
> ⚠️ 옛 지시는 "`PROJECT_STATE §0`에 남겨라"였으나 **「저장 위치」 계약과 충돌**한다(§0은 회고를 담지 않는다). 2026-07-20에 커밋 메시지로 옮겼다.
