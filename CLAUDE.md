# CLAUDE.md

Claude Code가 이 프로젝트에서 따라야 할 규칙.

---

## 세션 시작 시 필독

모든 작업 세션 시작 전에 반드시 아래 두 문서를 먼저 읽는다:
- `docs/PROJECT_STRUCTURE.md` — 페이지 구조, JS 역할, 데이터 흐름 (인덱스)
- `docs/PROJECT_STATE.md` — 완료 기능, 버그 목록, 추후 작업

`PROJECT_STATE.md §0(진행 중 작업)`에 문서가 명시된 경우 함께 읽고, 진행 중 항목이 있으면 복원 보고를 한다: 현재 목표 / 보류 항목(사유) / 미실행 항목 / 다음 작업 후보. 사용자가 정정하지 않으면 그대로 진행.

작업 주제가 바뀔 때마다 아래 표를 보고 해당 파일을 읽는다. **해당 파일을 읽기 전에 수정하지 않는다.**

| 작업 종류 | 필독 파일 |
|-----------|----------|
| DB 테이블/컬럼 추가·변경 | `docs/db-schema.md` |
| CottageDB 함수 / window 전역 API | `docs/js-api.md` |
| localStorage / 세션 키 | `docs/ls-schema.md` |
| 인증·로그인·프로필·사진 흐름 | `docs/PROJECT_STRUCTURE.md` §3~5 |
| 이용시간·체류 추적 | `docs/PROJECT_STRUCTURE.md` §6 |
| 게임 데이터·파이프라인 | `docs/PROJECT_STRUCTURE.md` §7~8 (작업 전 `npm run check`) |
| UI/CSS 디자인 | `docs/DESIGN_RULES.md` |
| 업적/칭호/캐릭터/교환권/성장보드 | `docs/achievement-system.md` (SSOT) |
| 관리자 분석(`requests-admin.html` 분석 영역)·방문/이용 집계·이벤트 퍼널 | `docs/admin-analytics.md` (SSOT) |

## 문서 관리

- 추후 작업 목록은 PROJECT_STATE.md에서만 관리. TODO.md 사용 금지 (있으면 삭제).
- 새 `*.md` 생성 전 기존 문서 흡수 가능성 먼저 검토. 일회성 기록은 PROJECT_STATE.md 또는 완료 후 삭제 우선.
- **PROJECT_STATE.md 슬림화**: 세션 시작마다 읽는 파일이라 **git으로 대체 가능한 완료·과거 서술이 활성 정보를 가리기 시작하면** 정리한다. **프루닝 트리거 — 아래 중 하나라도 참이면(줄 수 총량으로 판단하지 말 것)**: ⓐ §0 상단 세션 요약 블록이 **3개 초과** → 최신 2개만 남기고 나머지는 삭제(git log가 전부 보관, 복원엔 최근 2세션이면 충분). ⓑ 한 섹션에서 **닫힌 항목(✅종결·완료·`~~취소선~~`·`[x]`)이 열린 항목(`[ ]`·보류·판단)보다 많음** → 그 섹션의 닫힌 항목 삭제. ⓒ **순수 시간순 완료 로그(`최근 완료` 표·`변경 이력` 류)의 존재 자체** → 줄 수 무관·발견 즉시 삭제(정의상 git 중복). **줄 수(대략 800줄)는 위 셋을 "언제 점검할지" 촉발하는 값싼 알람일 뿐 그 자체가 프루닝 사유가 아니다** — 비율은 다 읽어야 재지므로 값싼 트립와이어가 필요할 뿐. 절차: ①위 ⓐ~ⓒ로 **git 로그와 중복되는 과거 기록은 삭제**(아카이브 `*.md` 생성 금지) ②단 그 안에 섞인 **열린 "다음 작업 후보"는 먼저 §3(추후 작업)로 추출** 후 삭제 ③완료 체크포인트는 미해결 하위 항목 없음 확인 후 제거. 교훈·회고는 소멸시키지 말고 CLAUDE.md 규칙으로 승격 후 삭제.
- 복원 비용이 높다고 판단되면 `*_CHECKPOINT.md` 생성을 제안하고, 승인 후 §0에 등록. 신호: 보류된 결정, 후보안 여러 개, 다중 파일 비교 분석, 대화에서만 있는 판단 근거, 장시간 판단 작업. 판단 기준은 작업 종류가 아니라 복원 비용.
- 서브파일 갱신은 **코드 변경과 같은 커밋에 포함**. "나중에 한꺼번에" 금지 — compact 시 날아감.
- **DB 테이블/이벤트/JS 파일 추가·변경 시 해당 docs 갱신을 같은 작업 안에서 수행한다.** feature 작업이어도 예외 없음.
- 커밋 순서: ① 코드 변경 → ② 해당 서브파일 즉시 갱신 (갱신 트리거 참조) → ③ PROJECT_STATE.md 반영 → ④ git diff 확인 후 커밋.
- 서브에이전트에 작업을 위임한 경우, 완료 후 문서 갱신 여부를 직접 확인한다. **서브에이전트는 CLAUDE.md 규칙을 따르지 않는다.**

### 문서-코드 동기화

- 버그·리팩토링 후보·체크포인트 항목을 해결하면 **원래 문제가 적힌 문서 위치**를 찾아 해결됨/보류/폐기로 닫는다. 같은 항목을 언급한 문서가 여러 개면 상태를 일치시킨다.
- 재검증만으로 "문제 없음" 판단한 경우도 근거를 남긴다. 예: "현재 함수 없음", "호출처 없음".
- **"기능·동작이 없다"는 grep 한 번으로 단정해 문서·커밋에 사실로 적지 않는다** (2026-07-17 교훈): 배경클릭 닫기가 실제로 [kakao-auth.js:1717](assets/js/kakao-auth.js#L1717)에 있었는데 "없다"고 커밋에 기재했다가 사용자 반박으로 발각. 부재 주장은 **실측·재현으로 확인 후** 기재 — 「검증 결과 0건이면 검사기 의심」의 문서 기재판.
- 완료 기록에는 **왜 다시 문제가 아닌지**를 짧게 쓴다.
- 커밋 전 `rg`로 항목 ID·핵심 함수명을 검색해 낡은 미해결 기록 확인. 해결된 항목이 미해결로 남아 있으면 문서 정합성 문제로 분류.
- "완료"는 코드와 문서가 함께 닫혔을 때만.
- **문서 참조도 소급 감사 대상이다** (2026-07-18). sync-on-code-change는 "바뀌는 자리"만 고치므로 인덱스 문서(`PROJECT_STRUCTURE.md` 파일역할·`js-api.md` 소비처 열·감사 항목의 참조 기재)의 **기존 드리프트는 구조적으로 못 잡는다** — 「새 규칙 추가 시 소급 위반 확인」의 **문서판**이다(실제: GS7 착수 시 감사 항목이 소비처를 오기재해 재탐색 유발, js-api.md의 `CottageGameView` 소비처에 script-nav.js 누락 — 표본 2/2 드리프트). 새 참조 표를 추가하거나 그 코드를 만질 때 해당 참조를 `rg`로 실제 코드와 대조해 드리프트를 §3에 등록한다. **문서 수정은 zero-blast-radius(런타임 무변경)**라 코드 소급 위반과 달리 "등록까지가 목적"이 아니고, 값이 싸면 그 자리에서 바로 고친다.

## 문서 갱신 트리거

| 변경 사항 | 갱신할 파일 |
|-----------|------------|
| DB 테이블/컬럼 추가·변경 | `docs/db-schema.md` |
| CottageDB 함수·window 전역 변경 | `docs/js-api.md` |
| localStorage 키/구조 변경 | `docs/ls-schema.md` |
| 페이지 이동/추가/삭제, 인증·데이터 흐름 변경 | `docs/PROJECT_STRUCTURE.md` |
| 관리자 분석 화면/지표 변경, 새 `trackEvent` 타입 추가 | `docs/admin-analytics.md` |
| 기능 완료·버그 발견·작업 추가 | `docs/PROJECT_STATE.md` |

## 외부 의견 수렴

GPT·Gemini 등 외부 AI/제3자 의견은 구현 지시가 아니라 감사 입력이다. 즉시 정답으로 간주하지 않고 현재 코드·문서·DB 기준으로 재검증한다. 차이가 있으면 먼저 보고하고 승인 후 구현. 출처 미명시여도 문체(구조화 목록, 프로젝트 문맥 없는 일반론)로 파악하고 동일 적용.

## 이용 규칙 관리

작업 중 운영 방식이 바뀌면: 이번 세션만 → PROJECT_STATE.md 메모 / 계속 적용 → CLAUDE.md 갱신 대상(Yellow 절차). 커밋 전 바뀐 운영 방식이 있으면 사용자에게 한 줄로만 질문. 목록 나열·긴 설명 금지.

### 새 규칙 추가 시 소급 위반 확인 (2026-07-16)

CLAUDE.md에 **코드에 관한 규칙**을 새로 추가하면, 그 자리에서 기존 코드의 위반을 grep해 **PROJECT_STATE §3에 등록**한다. 규칙은 대개 사고가 난 뒤에 생기는데 기존 코드엔 소급되지 않아, "규칙은 있는데 위반이 수십 곳" 상태가 조용히 쌓인다. 실제 사례(둘 다 2026-07-16에야 발견):

- 「DB 함수 에러 처리」(2026-07-08 추가) → `catch (_) { return []; }` 위반이 `supabase-client.js` 48곳 + `openProfilePanel` 11곳 그대로 남아 있었음
- 「Supabase RLS 상태 명시」(2026-07-08 추가) → 그 이전 마이그레이션 4개(테이블 8개)에 미적용

**즉시 고칠 필요는 없다** — 대부분 운영 중이라 무영향이고, 일괄 수정이 오히려 위험할 수 있다(호출부가 기존 동작을 전제로 함). **등록까지가 목적**이다. 안 하면 다음 감사에서도 안 잡힌다 — Phase 1~3 감사가 위 둘을 모두 놓쳤는데, 감사는 "코드를 읽고 이상한 것을 찾는" 방식이라 **"규칙 ↔ 코드 기계적 대조"와는 그물이 다르기 때문**이다.

**예외 — 다음 작업이 그 코드를 건드리면 "나중"이 아니다.** 등록된 소급 위반이 착수하려는 작업의 수정 범위와 겹치면, 그 부분만은 함께 처리할지 착수 전에 판단한다(예: R10b 방향 A는 재조회 실패가 조용히 빈 목록이 되는 위 에러 삼키기와 정면 충돌).

## Plan-Execute 기준

아래 중 하나라도 해당하면 Plan 먼저 작성 → 승인 → 실행:
- DB 테이블/컬럼 신규 생성 또는 변경 (되돌리기 어려움)
- localStorage 키/구조 신규 생성 또는 변경 (기존 재사용 가능한지 먼저 확인)
- window.CottageDB 공개 API 추가/변경/삭제 (다수 파일 의존)
- 인덱스·서브파일 확인 후에도 영향 범위 불명확

### Plan 형식

```
읽을 파일: [파일명:줄번호 범위]
변경할 대상: 기존 X → Y (재사용 or 신규, 신규면 이유)
새로 생성: 없음 / [대상 + 이유]
영향 파일: A.js, B.html
위험요소: 오탐/충돌/예외 + 첫 번째로 실패할 가능성이 높은 지점
```

## 신규 생성 원칙

리팩토링의 목적은 분리가 아니라 규모에 맞는 구조 최적화다. 유지·통합·분리·삭제 중 가장 단순한 해결책 우선.

새 localStorage 키, DB 테이블, 전역 함수, CSS 클래스 생성 전:
1. `ls-schema.md`, `db-schema.md`, `js-api.md`에서 동일 용도 기존 항목 검색
2. 기존 확장이 가능하면 그 방향으로
3. 신규 불가피하면 이유를 Plan 또는 커밋 메시지에 명시

## 구현/리팩토링 분리

기능 구현과 리팩토링은 같은 작업 단위에서 섞지 않는다.

- **구현 단계**: 동작하는 기능 완성 우선. 단, 역할 명확한 헬퍼 함수는 추출, 1회성 로직 과도 분리 금지, 기존 코드 스타일 유지, 중복 발견 시 즉시 대규모 정리 금지. 리팩토링 후보는 PROJECT_STATE.md §0 `코드 품질 주석`에 메모.
- **리팩토링 단계**: 기능 테스트 완료 후 별도 세션. REFACTOR MODE 규칙 준수. 신규 기능·UI 변경 금지.

## 작업 모드

작업 시작 전 완료 조건을 한 문장으로 정의한다. 예: "카드 클릭 시 바텀시트가 열린다."

- **BUG FIX MODE** 금지사항: 리팩토링·구조 개선·분리·명명 변경·CSS 정리, 다른 문제 수정(보고만), **git revert(멈추고 사전 설명 → 승인 필수)**
- **REFACTOR MODE** 금지사항: 신규 기능, UI 변경, 요구사항 변경, 발견한 기존 버그 수정 — 보존하되 "원래 그런 동작(quirk)"으로 단정하지 말고 **버그 가능성을 함께 보고**한다(고칠지 판단은 사용자 몫). 보존한 동작을 코드 주석·문서에 "의도된 사양"처럼 적는 것도 금지.

## 모델/effort 선택 규칙

- 그린(표기·단순UI·verify): Sonnet medium
- 옐로(명확한 버그·소규모 기능): Sonnet high 또는 Opus medium
- 레드·원인불명 버그(재현불가·간헐적·가설실패 이력): Opus xhigh, 수정 전 원인 조사 단계 분리
- Plan 모드 필수 작업(RLS, 묶음 알림 등): Opus xhigh 고정
- max: 규칙 외. xhigh로 2회 실패한 문제에만 세션 한정 사용

### 모델 전환 타이밍 (정지 후 사용자에게 요청)

원칙: 기획·분류·원인조사·Plan은 Opus, 실행(그린·명확한 옐로)은 Sonnet. 아래 시점에 **현재 완료분을 커밋한 뒤 작업을 멈추고** 사용자에게 모델 전환을 명시적으로 요청한다 (자동 전환 불가, 사용자가 `/model`로 전환).

1. **분류/Plan/기획/원인조사 완료 → 실행 진입 직전**: Opus로 방향 확정 후, 남은 실행이 그린/명확한 옐로면 정지 → "Sonnet 전환" 요청.
2. **Sonnet 실행 중 상위 티어 도달**: Red(DB/구조/파일삭제)·원인불명 버그(재현불가·간헐·가설실패 이력)·Plan 필수 항목을 만나면 그 항목 시작 전 정지 → "Opus 전환" 요청. 해당 항목만 분리하고 나머지 그린/옐로는 계속 진행.
3. **티어 경계**: 한 배치(예: 그린) 완료 후 남은 게 상위 티어(옐로/레드)면 정지 → 다음 티어에 맞는 모델을 명시해 전환 요청.
4. 미완 상태로 전환 넘기지 않는다 — 정지 전 반드시 원자 커밋으로 마감.

## WIP 제한

활성 작업(WIP)은 동시에 3개 초과 금지. 작업 중 떠오른 아이디어는 PROJECT_STATE.md 다음 작업 후보에 기록만.

## 버그 수정 방식

- 증상 → 관련 함수 → 파일 순으로 가설을 먼저 텍스트로 정리.
- **읽을 파일 1~2개로 한정.** "관련될 수 있는 모든 파일" 전체 탐색 금지.
- 방문자·통계 데이터 버그 수정 시 데이터 초기화 금지. 원래 있어야 할 값으로 복원하는 방식으로만.

### 파일 읽기 전 경로 확인

1. `PROJECT_STRUCTURE.md`로 파일 역할·경로 확인
2. 작업 종류별 docs로 함수명·줄번호 확인
3. Grep으로 줄번호 확인 → 해당 줄 ±30줄만 Read. 파일 전체 읽기 금지.

### 반복 패치 정지

같은 파일·함수·CSS 선택자를 **2회 이상** 수정해도 해결 안 되면 즉시 멈추고 보고: "2번 수정했는데 해결이 안 됩니다 — 접근 방향이 틀렸을 수 있습니다."

**3회차부터 코드 수정 금지.** 원인 가설 / 검증 방법 / 변경 예정 파일 / 롤백 방법 먼저 보고, 승인 후 수정.

검증·테스트 스크립트에도 동일 적용: 같은 스크립트를 2회 이상 수정해도 실패가 반복되면 스크립트가 아닌 구현 로직·환경 전제를 의심하고 먼저 보고.

### 런타임 값 검증

레이아웃·스크롤·sticky·position·offset·CSS 변수 관련 버그는 추론으로 수정하지 않는다. 수정 전 실제 런타임 값 확인: `getComputedStyle()`, `getBoundingClientRect()`, `offsetHeight`, `clientHeight`, `console.log()`.

- 2회 실패 → 런타임 값 → 실제 DOM 구조 → 원인 확인 후 수정.
- `calc()`/`var()`/`env()` 포함 CSS 변수는 `parseFloat()` 금지(NaN 위험). 실제 DOM 크기를 재거나 CSS가 처리하게 둔다.
- 브라우저/CSS 렌더링 버그는 값 조정 전 구조 분리 가능성 먼저 검토.
- "수정 완료"는 실제 재현 확인 결과와 함께만. 확인 못 했으면 "완료" 금지.

### UI 요소 '안 보임' 버그 디버깅 순서

dropdown/tooltip/modal이 안 보일 때 JS 코드 탐색 전에:
1. CSS 클리핑 먼저 확인: 부모 체인에서 overflow:hidden/auto/scroll 찾기
2. z-index 확인: position:fixed/absolute 요소가 다른 stacking context에 묻히는지
3. display:none / visibility:hidden 여부

JS 탐색은 그 다음. "붙었는지"가 아니라 "보이는지"로 증상을 먼저 분류할 것.

새 등록 vs 수정 같은 "같은 코드, 다른 결과" 패턴:
→ 상태 차이(empty vs has-data) 기준으로 코드 실행 경로 분기점을 먼저 찾고,
  그 분기점 이후에 레이아웃이 달라지는 CSS 쪽도 함께 확인.

### CSS/sticky/바텀시트 원칙 (2026-07-03 교훈)

sticky·scroll·bottom sheet·fixed header·iframe sheet·border-radius·overflow 버그는 수치 문제처럼 보여도 실제 원인은 레이아웃 구조인 경우가 많다.

1. **사용자가 말한 기준 컴포넌트를 다시 확인한다.** 최근 수정한 값을 기준으로 삼지 말고, 현재 요청의 비교 대상을 다시 확인.
2. **공백이 스크롤 영역인지 고정 영역인지 먼저 구분한다.** `헤더1+공백+헤더2`가 함께 고정돼야 하면 공백도 sticky/fixed 영역에 포함.
3. **패널 시작 위치·height·sticky top을 건드리지 말라는 요청은 그대로 지킨다.** 고정되는 영역 자체의 padding/margin만 조정. 위치·height 변경은 다른 버그를 만든다.
4. **같은 선택자를 2번 이상 건드려도 안 되면 값 추측을 멈춘다.** DevTools 런타임 값으로 확인 후 수정.
5. **커밋 전 diff에서 의도 밖 선택자가 바뀌었는지 선택자 이름까지 확인한다.**

### 닫기 애니메이션·show-then-hide 깜빡임 (2026-07-18 교훈)

"열 땐 멀쩡한데 **닫을 때 뭔가 한 프레임 켜졌다 꺼지는**" 깜빡임의 두 원인. 실제 사고: 홈 플래너 빠른진입 모달을 닫으면 플래너가 순간 떴다 사라짐(1차 수정 실패 후 2차에 규명, 커밋 511c15e).

1. **transition 진행 중 상태 클래스 제거 금지.** opacity/transform transition이 걸린 요소를 **닫는 도중** 상태 클래스를 떼면 요소가 '기본 모습'으로 복귀해 애니메이션되며 깜빡인다. 실제: `closeModal`이 `is-open`(opacity 0.25s 페이드)과 `is-quick-entry`를 **동시 제거** → 페이드 도중 quick-entry가 빠져 `.planner-sheet-panel`이 투명·풀스크린에서 기본 `background:#fff`·480px 박스로 복귀하며 슬라이드·페이드. **해결: 닫기에선 상태 클래스를 안 떼고 다음 정상 오픈에서 정리**(요소가 완전히 사라진 뒤 떼야 페이드에 안 걸림).
2. **show-then-hide(전체 로드 후 일부 숨김) 패턴은 컨테이너 전체를 숨겨라.** 일부만 숨기면 나머지가 닫을 때 드러난다. 실제: `is-quick-entry`가 `#viewWeek`만 숨겨 브레드크럼·페이지 헤더가 남아 오버레이 닫을 때 노출 → `.inner-page` 전체를 `display:none`으로 바꿔 해결. 사용자 표현 "**애초에 안 켜지게**"가 이 뜻(켜놓고 지우는 게 아니라 아예 렌더 안 되게).
3. ⚠️ **1차 실패의 뿌리 = 추론으로 먼저 고침.** iframe `#viewWeek` 토글 타이밍만 가정해 옮겼다가 실패 — 진짜 원인은 부모 패널의 transition+배경 복귀였다. 「런타임 값 검증」의 재확인: **닫기 깜빡임은 닫는 요소의 CSS(transition 있는가·상태클래스가 뭘 되돌리는가·숨김이 완전한가)부터 읽을 것.** 그걸 먼저 읽었으면 1차에서 잡혔다.

### 모달/iframe 재사용 원칙 (2026-07-07 교훈)

홈에서 iframe으로 열리는 모달(기록·플래너 등)은 재사용 구조이므로 직전 상태가 잔류한다.

1. **open 시점에 목표 상태 전체를 선언한다.** 뷰 모드·주차·목적지 등 원하는 최종 상태를 postMessage 하나로 선언. close 시 청소나 부분 상태만의 리셋은 누락 경로(ESC·백드롭 등)가 생긴다.
2. **src 속성 비교로 재로드 스킵 금지.** iframe 내부 탐색 시 src 속성값은 바뀌지 않는다. 실제 현재 위치 확인은 `contentWindow.location.pathname` 사용.
3. **조건부 재로드.** 같은 페이지에 머물렀으면 postMessage로 탭/상태 전환, 다른 페이지로 이탈했으면 src 재설정.
4. **닫기 경로는 한 함수로 모은다 (2026-07-16 교훈).** dim(백드롭)·✕·ESC가 각자 닫기를 호출하면 레이어 가드(예: 라이트박스 먼저 닫기)가 한 곳에만 들어가고 나머지에서 누락된다. "닫기 요청은 항상 가장 위 레이어 하나만" 규칙을 한 함수(`closeTopLayer` 등)에 넣고 세 경로가 공유.
5. **iframe 경계에서 z-index로 클릭 가능성 추론 금지 (2026-07-16 교훈).** 부모 문서의 요소(모달 ✕ 등)는 iframe **내부** z-index로 가려지지 않는다. iframe 안 오버레이가 `z-index:9999`여도 그 위에 겹쳐 보이는 부모 버튼이 실제로 눌린다. "위 레이어라 밑은 클릭 불가"라는 추론은 같은 문서 안에서만 성립 — 크로스 도큐먼트는 실제 클릭 대상을 이벤트로 확인할 것. 사용자가 "특정 조건에서만"이라고 짚은 반례를 z-index 이론으로 반박하지 말 것.
6. **"닫고 전환"과 "겹쳐 쌓기"를 구분한다 (2026-07-17 교훈).** A에서 B를 열 때 **판단 기준은 사용자가 B를 닫은 뒤 무엇을 기대하는가** 하나다. "닫으면 A가 그대로 있어야지"라고 느끼는 관계면 그건 전환이 아니라 **레이어**다 → **A를 닫지 말고 B가 위에 뜨게** 한다(= z 순서 문제). A를 지우고 B를 연 뒤 복귀 장치(뒤로가기·`backTo`)를 다는 건 **오답**이다 — 복귀 버튼을 안 누르고 닫으면 A가 사라지고, 애초에 사용자는 "전환"한 적이 없다. 실제 사고: 이날모임 상세 → 닉네임 → 보드에서 보드가 모달보다 z가 낮다는 이유로 모달을 `remove()`했다가 "보드 닫으면 상세가 없어진다"는 지적을 받았고, 이어 `backTo`를 붙이려다 또 반려됐다. 정답은 **그 모달만 보드 아래 z로 내리고 안 닫는 것**이었다.
   - **z가 안 맞으면 z를 고친다 — 단 공유 클래스 전체가 아니라 그 인스턴스만.** `.dd-overlay`(9200)를 통째로 낮추면 **모임보드 서브시트 안에서 열리는** `openDatePreviewModal`이 뒤에 깔린다 → `openDateMeetingModal`에만 `.dd-overlay--under-board`(9050)를 줬다. **같은 클래스를 쓰는 다른 호출부가 "무엇 안에서" 열리는지 먼저 grep할 것.** (R10c 「z를 올려 고치지 말 것」의 대칭 — **내려도 깨진다**.)
   - **반대로 정당한 `close()`도 있다**: B가 A보다 z가 높을 수 없는 구조적 이유가 있고(예: 박스모달 9700 > 게임시트 9500) 사용자도 복귀를 기대하지 않는 자리. 이땐 닫는 게 맞다. **규칙은 "닫지 마라"가 아니라 "닫기 전에 레이어로 풀리는지 먼저 보라"이다.**

### DB 함수 에러 처리 (2026-07-08 교훈, 2026-07-17 원칙 재정립)

**원칙: 고칠 게 있는 실패만 로그한다.** 기준은 하나 — **"이게 울렸을 때 내가 고칠 게 있는가?"**
- **있다** → `console.error('[실패 지점 이름]', e)` 후 반환. `catch (_) { return []; }`로 조용히 삼키지 않는다. (예: DB 오류, 손상된 localStorage)
- **없다** → 삼키되 **왜 삼키는 게 맞는지 주석**을 남긴다. (예: `getUserPhotoCount`의 `JSON.parse` fallback — 사진 1장은 단일 URL 문자열이라 parse 실패가 정상 분기)

감지기의 가치는 **"울리면 진짜"**에서 나온다. 조용한 실패는 화재를 못 보게 하고, 정상 경로마다 울리는 로그는 진짜 화재를 가린다(늑대소년). **둘은 같은 원칙의 양쪽 위반이지 규칙과 예외가 아니다.**
  - ⚠️ 이 원칙은 원래 **"항상 로그"**였고, 그게 틀려서 2026-07-17에 고쳤다. 틀린 버전을 codemod가 성실히 적용해 위 `JSON.parse` fallback에 로그가 달렸고 **정상 기록마다 가짜 에러**가 찍혔다(호출 1회에 9건, 019bd7c에서 제거). **규칙이 부정확하면 기계적 적용이 그걸 증폭한다.**
  - **라벨은 실제 실패 지점의 이름으로.** 틀리면 디버깅 때 엉뚱한 함수로 사람을 보낸다. 중첩 함수·객체 메서드 안에서 바깥 함수명을 쓰기 쉽다(실제: `get(uid)` 안의 catch가 `[_migrate]`로 표기, 019bd7c에서 수정).
- ⚠️ **`try/catch`만으론 부족하다 (2026-07-17 실측 정정)**: **supabase-js는 쿼리 오류에 예외를 던지지 않고** `{ data: null, error }`를 반환한다. 그래서 `const { data } = await db.from(...)` 형태면 **컬럼 오타·RLS 차단·테이블 없음이 전부 조용히 빈 배열**이 되고 `catch`도 `console.error`도 안 울린다(= 가장 흔한 실패가 감지 불가). **`const { data, error }`로 error를 받아 `if (error) console.error('[함수명]', error)`** 할 것. try/catch는 네트워크 장애·JS 예외 전용. 상세는 [docs/js-api.md](docs/js-api.md) "CottageDB 에러 처리 규약".
- 🚨 **`error`를 받아도 부족하다 — 행 수 자체가 거짓말한다 (2026-07-18, 하루에 두 건 발각)**: 아래 둘은 **`error`가 `null`이고 `catch`도 안 걸린다**. 감지기 1~4단계를 전부 돌린 뒤였는데도 못 잡았다 — 실패가 **에러가 아니라 "그냥 데이터가 이만큼"**의 모습을 하고 있기 때문이다.
  - **행 수가 정확히 `1000` → 절단을 의심**. PostgREST `max-rows` 상한이고 **클라이언트 `.limit(20000)`으로 못 넘는다**(실측). 실제 사고: `getPageAnalytics`가 90일을 요청하고 1,000행을 받아 **11,439행 중 91%가 조용히 사라졌다**(= "90일 분석"이 실제로는 7일). 관리자 화면의 총 체류시간이 **12시간으로 보였으나 실제 276시간**.
  - **행 수가 `0` → RLS를 의심**. 정책이 아무에게도 매치되지 않으면 **에러가 아니라 빈 결과**가 온다. 실제 사고: `page_events`가 `SELECT ... to authenticated`로 걸려 있었는데 이 프로젝트엔 **`authenticated` 역할이 존재할 수 없어**(카카오 OAuth) 1,452행이 전부 안 보였다. 이벤트 퍼널이 "데이터 없음"으로 몇 주간 방치됨.
  - **판정법**: `count: 'exact'`로 **실제 행 수와 대조하기 전엔 "데이터 없음"으로 결론짓지 않는다.** 없는 테이블은 `count`가 `null`, 비었지만 존재하면 숫자 `0` — 이걸로 구분된다.
  - **정책을 쓸 땐 `to` 역할이 `anon`인지 확인**한다. 이 프로젝트에서 `authenticated`·`auth.uid()`는 항상 죽은 코드다. (기존 「RLS 상태 명시」 규칙은 *상태 미명시*만 잡고 *역할 오지정*은 못 잡았다.)
  - ⚠️ **`max-rows`는 대시보드 설정이라 마이그레이션에 안 남는다** — DB 재구축 시 1000으로 돌아가 증상이 조용히 재발한다. 상세는 [docs/db-schema.md](docs/db-schema.md) "PostgREST max-rows".
- **반환 계약은 유지**한다 — 실패 시 `[]`/`null` fallback을 에러 throw로 바꾸면 빈 배열을 전제로 렌더하는 호출부가 깨진다. 바꾸는 건 **관측(로그)이지 동작이 아니다**.
- **reason 정확성**: 구체적 실패(not_found, invalid 등)를 일반적 실패(max_priority 등)보다 먼저 검사한다. 순서가 뒤바뀌면 잘못된 reason이 반환되어 UI 분기가 오동작한다.
- **Supabase RLS**: 테이블 생성 시 항상 RLS 상태를 명시한다. 이 프로젝트 기본은 `DISABLE`(카카오 OAuth 구조상 auth.uid() 불가, anon 키 직접 read/write). Supabase는 신규 테이블에 RLS를 자동 활성화하므로 마이그레이션 SQL에 `ALTER TABLE {name} DISABLE ROW LEVEL SECURITY;` 포함. 실행 직후 anon 키 SELECT로 접근 확인.

## 커밋 전 검증 (필수)

"될 것 같다"는 느낌으로 커밋하지 않는다. 아래 중 하나 이상으로 실제 동작 확인 후 커밋:

- **로직 추적**: 실제 데이터 값으로 한 단계씩 (날짜·시간 계산, DB 쓰기, 집계)
- **node 테스트**: 서버사이드 로직은 스크립트 실행으로 출력 확인
- **엣지케이스**: 0값, null, 날짜 경계, SELECT 실패 등 명시적 확인
- **UI/CSS는 브라우저 직접 확인 필수.** 시각적 버그는 눈으로 확인 전까지 완료 아님.

**Plan이 필요했던 작업은 커밋 전 사용자 시점으로 한 번 더 훑는다.** 목적은 의도와 실제 동작의 불일치 확인 (예: 배지가 숨겨진 버튼에 달림, 패널 열면 알림 사라짐).

### 검증 결과가 "0건"·"전부 통과"면 검사기 자체를 먼저 의심한다 (2026-07-16 교훈)

안심되는 결과일수록 도구가 고장 난 것과 구별되지 않는다. **알려진 실패 케이스를 일부러 심어 잡히는지 확인(음성 대조군)하기 전까지 통과를 신뢰하지 않는다.** 하루에 5번 조용히 거짓말한 실제 사례:

- **bash heredoc·`node -e` 큰따옴표는 정규식 백슬래시를 먹는다** — `'\\b'`가 `'\b'`(백스페이스 문자)가 돼 **어떤 이름도 매치 불가** → "문제 0건". 검사 스크립트는 **Write 툴로 파일 생성**할 것.
- **DB/HTTP 조회는 상태 코드를 확인한다** — 컬럼명 오타로 PostgREST가 HTTP 400을 줬는데 이를 **"0행"으로 오독**("page_sessions 0행" → 실제 10,975행).
- **컬럼·필드명은 추측하지 말고 원본에서 확인한다** — 마이그레이션 SQL·db-schema.md를 볼 것. 추측한 이름으로 조회해 "마이그레이션 미적용"이라는 **거짓 결론**이 나올 뻔했다(실제 이름은 `is_priority`/`player_condition`).
- **문자열·주석 안의 이름을 코드 참조로 세지 않는다** — `'.profile-panel-stats'`, `document.body` 같은 것이 오탐을 만든다.
- **손으로 만든 스캐너로 JS를 렉싱·파싱하지 않는다** — 파일을 감싼 IIFE `(function () {`를 "`function`이라는 이름의 함수"로 세고, 정규식 리터럴 `/"/g`의 `"`를 문자열 시작으로 읽어 여러 줄을 삼킨다. 둘 다 "141개 중 138개 불일치"라는 그럴듯한 거짓 결과를 냈다(2026-07-17). 답은 대개 질문을 좁힌 grep 한 줄이다(실제: "이 파일에 중첩 함수가 몇 개냐" → 2개 → 오라벨 1건 확정).

- **이 환경의 Grep은 `content` 출력에서 `/`를 `\`로 표시한다 (2026-07-19)** — `Math.round(a/b*100)`이 `a\b`로, `// ──` 주석이 `\ ──`로 보인다. **JS 문법 오류로 오인해 "깨진 코드 발견"이라 보고할 뻔했고**, Read로 원본을 확인해 아티팩트임을 규명했다. **Grep 출력의 슬래시는 근거로 쓰지 말 것** — 슬래시가 걸린 판단은 반드시 Read로 재확인.
- **브라우저 자동화 산출물을 리포 안에 쓰지 말 것 (2026-07-19, 2회 실패 후 규명)** — 개발 서버(Live Server 5500)가 리포를 감시하므로 **PNG를 리포 안에 쓰는 순간 라이브 리로드**가 걸려 페이지가 첫 화면으로 초기화된다. 증상은 `boundingBox=null` → 스크린샷 30초 타임아웃으로 나타나 **"탭이 안 열린다"처럼 보이고**, 그래서 스크립트를 고치게 된다. 원인은 **출력 경로**다 → OS 임시폴더로. 「반복 패치 정지」의 "스크립트가 아니라 **환경 전제**를 의심하라"가 정확히 이 사례였다.

**모순을 만나면 그게 단서다.** 위 거짓 결론들은 전부 "이미 아는 사실"(스모크가 통과했었다, 기능이 운영 중이다)과 어긋나서 발각됐다. 검사 결과가 알려진 사실과 충돌하면 **사실을 의심하기 전에 검사기를 의심할 것.**

**자동 검증 통과는 육안 확인을 대체하지 않는다 (2026-07-19, 두 세션 연속 재현).** assert가 전부 PASS인 뒤에 스크린샷을 보고 잡은 게 세션 ⑫ #8(짧은 막대에서 흰 글씨가 막대 밖으로 넘쳐 안 읽힘)과 세션 ⑬ #21(회원 카드 헤더 38분 vs 펼침 표 61분)이다. **assert는 내가 물어본 것만 답한다** — 화면에는 내가 물어볼 생각을 못 한 것이 같이 떠 있다. UI를 건드린 커밋은 스크린샷을 **읽을 것**(찍는 것과 다르다).

## 영구 식별자(Immutable ID)

배포 후 변경 금지 — 사용자 자산(캐릭터, 도감, 포인트)의 기준:
- `achievements.id` (예: `record_1`, `new_game_20`)
- `game_play_records.game_id` (BGG ID)

변경 가능: 게임명, 설명, 이미지, 업적 이름·포인트. 변경 불가: ID 자체. BGG ID 정정으로 기존 기록의 game_id를 바꾸지 않는다(신규 게임에만 새 ID). **DB 변경 전 "기존 사용자가 무엇을 잃는가?"를 먼저 확인.** 변경이 불가피하면 기존 ID 유지 + migration 계획 + 데이터 이전 방안 설계 후 승인 필요.

---

## 작업 권한

**Green** — 승인 없이 진행: CSS 값 변경(color, padding, font-size 등 수치·값만), 문구·오타 수정, 배너 교체, PROJECT_STATE.md 사실 기록 갱신

**Yellow** — 변경안 보고 후 승인: CSS 구조 변경(선택자 스코프·클래스 추가·제거·`position`·`z-index`·`display`·신규 추가·삭제), CLAUDE.md·DESIGN_RULES.md 수정, 신규 `*.md` 생성, PROJECT_STATE.md 구조 변경

**Red** — Plan 후 승인: DB, 인증, 데이터 구조, 파일 삭제, 대규모 리팩토링

## 배치 처리 (Batch Mode)

Yellow/Green 등급 작업 여러 건을 한 지시로 받으면 건별 승인 왕복 없이 연속 처리한다.

- 각 항목은 별도 커밋 (atomic 원칙 유지)
- 전체 완료 후 한 번에 보고: 항목별 커밋 해시 + 변경 요약 + UI 변경분 스모크 테스트 포인트
- Red 등급 항목은 배치에 포함 불가 — 발견 시 해당 항목만 분리해 Plan 절차로 전환하고 나머지는 계속 진행
- 배치 중 항목 하나가 실패/막히면 그 항목만 보류 보고하고 다음 항목 진행 (전체 중단 금지). 단, 뒤 항목이 실패 항목에 의존하면 함께 보류
- "확인 후 구현" 조건이 붙은 항목은 확인 결과가 부정이면 구현하지 않고 보고만 한다

### 작업 완료 보고

진행 중 작업이 여러 개이거나 미실행 항목이 있는 경우, 커밋 후 구분 보고:
1. 변경된 파일/내용
2. 미실행 항목
3. 다음 후보 작업

- `db-schema.md`, `ls-schema.md`, `DESIGN_RULES.md`, `PROJECT_STRUCTURE.md` 중 하나라도 갱신된 커밋이면, 완료 보고에 "⚠️ 웹 프로젝트 지식 재업로드 필요: [파일명]"을 포함한다.

## Git 작업

- 작업이 완료될 때마다 자동으로 커밋한다.
- commit 후 자동으로 push하지 않는다. push는 사용자가 명시적으로 요청할 때만 한다.
- 이 저장소에는 Claude Code 세션을 동시에 하나만 돌린다 (병렬 세션 금지).
- 한 커밋에 feat와 refactor 변경을 섞지 않는다.
